// Copy to _config.js and fill in — _config.js is gitignored and must never
// be committed. Vercel deploys include it; the repo does not. The env var
// OPENROUTER_API_KEY, if set on the project, takes priority over this file.
export const OPENROUTER_KEY = "";
