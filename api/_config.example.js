// Copy to _config.js and fill in — _config.js is gitignored and must never
// be committed. Vercel deploys include it; the repo does not. Env vars
// (OPENROUTER_API_KEY, SUPABASE_URL, SUPABASE_KEY) take priority if set.
export const OPENROUTER_KEY = "";
export const SUPABASE_URL = "";
export const SUPABASE_KEY = "";
