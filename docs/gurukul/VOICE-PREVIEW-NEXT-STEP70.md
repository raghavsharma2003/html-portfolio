# Existing owner voice: next executable step

2026-09-08, source de84da66 plus voice-path70. Source inspection only; no owner audio, consent, SQL, cloud endpoint or GPU call was read or changed.

The next product action is one protected Hindi preview after a current owner/reference audit and an admitted GPU window. The existing caller is already present. This patch only prevents missing protection configuration from being discovered after GPU synthesis.

## Owner route and prerequisites

Open `/studio`, sign in as the actual owner, select the clone, then **Meet > Voice sample > Hindi**. The workspace mounts `VoicePreviewPanel.tsx`, whose API caller posts to `/api/voice-preview`. Hinglish maps to `language_id=hi`; English maps to `en`.

`beginOwnedVoicePreview` requires the authenticated owner, eligible self replica, current age/identity/liveness verification, matching unexpired scopes, ready solo source, requested draft genome, and selected enhanced artifact included in that genome. Do not write consent rows or impersonate the owner. If verification is unavailable, report a platform blocker.

Before synthesis, root must bind the selected artifact ID/hash and original bandwidth/sample geometry, transcript review status, genome/version, and current consent/identity expiry and epoch. No such owner binding was established here.

## Configuration and bounded call

The V4 preview draft has 15 secret references, including the existing protection and voice secret names. It omits these public settings:

- `AZURE_OPEN_VOICE_ORIGIN=https://vyakti-open-voice-admission.purpletree-6dea69e2.centralindia.azurecontainerapps.io`
- `OPEN_VOICE_MODEL_ARM=general`
- `AZURE_AUDIO_PROTECTION_ORIGIN`: bind the existing protection service's independently verified external origin.

Voice needs the existing matching `OPEN_VOICE_HMAC_SECRET`. Local protection construction also validates the audio protection HMAC, watermark token, commitment secret, and optional PCM limit. A constructor pass verifies local configuration, not remote readiness or key equivalence. The preparation callback performs no network request.

The shared runtime is min0/max2. Historical idle metadata does not establish exclusive control. Root must admit an allocation window before any signed runtime-status or synthesis request. Preserve the unknown GPU hold. Do not warm, stop, raise minimum replicas, or dispatch from this document alone.

Within that admitted window, use one exact short Hindi sentence, preserve its durable intent and protected result/receipt, and record an owner judgment. Then one continuous Hinglish and one Indian-English sample may extend listening if the same admission permits them. A pending response is not permission to regenerate; the existing browser resumes the same intent.

## A baseline is not yet a matched pack

The ordinary server selects references using language-sensitive ranking and derives seed from replica, genome, language and text. UI clips do not prove the shared reference and seed required by the frozen comparison. Compare actual generation rows and receipts.

Reuse the frozen screening contract and non-dispatch packet for a matched comparison. The historical pack runner is not the owner-authorized product caller, while the newer recorded-ingest benchmark deliberately refuses before media reads. An independently reviewed real authority/protection adapter and exact frozen reference/seed bindings remain missing; do not add a permissive verifier or relabel three downloads as an accepted pack.
