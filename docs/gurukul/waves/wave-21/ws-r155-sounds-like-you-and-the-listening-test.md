# WS-R155: 'Sounds like you' and the listening test. The fidelity score the voice-evidence stack already computes becomes a product feature on Meet (a number, its date, what moved it, honest 'not measured yet'), and a blind listening test in the studio lets the owner compare two candidates of their own voice and record a preference that gates activation; the sealed listening harness and matched packs are reused, never a second scorer. Migration 166.

Read scratchpad w21/ws-common.md FIRST (the file the launcher names); every
rule there binds. Your worktree is checked out at the wave-twenty-one base
482e54b (verify with `git log --oneline -1`). The gate is 24 checks without
NEON_URL; run touched suites while you build and the full gate ONCE at the
end. Ten siblings build beside you on this machine: R151 HumanOS (the person
sheet, migration 163), R152 Deploy for a personal AI, R153 EmotionOS (vibe and
register, migration 164), R154 RelationOS in the Room (migration 165), R155
"Sounds like you" and the listening test (migration 166), R156 voice replies
that start fast, R157 the Vyakti mobile app, R158 the personal journey
rehearsed, R159 the personal studio in Hindi, R160 the Room and the landing
for any person. Migration numbers are ASSIGNED; never take another. Keep your
edits inside the files your brief names and append-only in shared files so
the main loop can merge mechanically. Do not spend money, do not touch the
live database, never print or commit a secret, never kill a process by
pattern, never `git stash`, never end a turn waiting on a monitor: gate in
the foreground with a timeout, commit, `git status` clean, full report in the
same turn.

## Product

The owner reweight names the fidelity guarantee as a product feature and
the voice benchmark names owner acceptance as the promotion condition.
Both exist as evals and tables, neither as a screen.

Laws:
1. Read `api/_replica-voice-*.js` (calibration, preview, preference,
   delivery policy), `vy_voice_fidelity` and `vy_replica_calibration` in
   db/schema.sql, `evals/voice-listening-benchmark`, `evals/voice-matched-pack`,
   `docs/gurukul/VOICE-LISTENING-BENCHMARK.md`, `research/EXPERT-VOICE-BENCHMARK-2026-09-06.md`,
   and rejected.md on cosine scores not settling likeness FIRST.
2. "Sounds like you" on Meet's voice sample view: the latest fidelity
   score for the current primary voice (speaker-embedding similarity from
   the evidence stack) as a 0..100 number with its date and reference,
   the owner's own last blind preference beside it, and three honest
   states: measured, not measured yet (why, and what triggers it), stale
   (the voice changed since). Never a fabricated number; a fixture without
   a score renders the honest state.
3. The listening test: two candidates of the owner's voice (existing
   preview artifacts) played blind in random order, four axes (likeness,
   naturalness, accent, pronunciation) on the sealed four-axis form, the
   verdict stored as a calibration row bound to both artifact hashes and
   the reference hash; migration 166 adds only what `vy_replica_calibration`
   lacks for a paired blind verdict (a `pair_sha256` and `winner_artifact_id`
   if absent), owner lane, erasure, export sentence, relcheck.
4. Activation of a new voice reads the latest verdict: a losing candidate
   cannot become primary without an explicit override that is logged.
5. Offline: the screen runs over fixture artifacts (short synthetic WAVs
   generated by the suite, never real voices); Chromium proves playback
   order is randomized and hidden until the verdict; the door battery cases
   the verdict op; nothing here calls Azure, and the report names the Azure
   run that would make the number real.

## Build

- db/migrations/166_*.sql (only if needed), db/schema.sql, api/_replica-calibration.js,
  api/replica-calibration.js, api/_replica-voice-preview.js (read of the
  score), src/studio/VoicePreviewPanel.tsx (the score), src/studio/
  ListeningTest.tsx (new + css, copy), CloneExperience.tsx (entry from the
  voice room), evals/listening-test (new), evals/room-doors, evals/run.mjs,
  layout/accessibility fixtures `studio:listening`.
- context/: decisions with reversal, measurements, rejections.
- Migration 166 only if needed. No new env var.
