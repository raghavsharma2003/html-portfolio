# Wave twenty-one (2026-09-13): the three OS layers, Deploy for any person, voice, mobile, Hindi

Ten workstreams, WS-R151 to WS-R160, built by ten parallel agents over the
repair commit `482e54b` (the tip after the gate passed 24 of 24 on the merged
handoff206 tree) and merged one at a time under the full gate, the way
docs/gurukul/waves/wave-20/README.md describes. `ws-common.md` is the
shared brief every agent reads first; each `ws-r1NN-*.md` is one
workstream. Migration numbers: 163 (R151), 164 (R153), 165 (R154, only if
needed), 166 (R155, only if needed).
