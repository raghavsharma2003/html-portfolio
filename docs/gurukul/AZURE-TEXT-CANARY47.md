# Azure text correction canary47: source preparation only

2026-09-08. No SQL, model call, budget change, deployment or Docker execution performed by this lane. This is a reviewed-source proposal, not an executable live launcher. Root must approve a separately frozen one-use launcher and exact synthetic fixture packet after dependencies are integrated.

## Scope and source prerequisites

Base materializer41 a4bf870fd3b8fffa5862081f03c32abba2ffaac5. This patch adds optional strict correction revision binding. Coordinate activation44/156 amendment for persisted candidate_core_hash; do not use old41 as evidence of that new binding. Root DEV target is vyakti_expert_integration_20260906 only. Fresh catalog must verify145 through154 as applicable, including actual152 and153;155 and156 need independent parser/runtime review, commit and fresh applied readback before the amended materializer is used. No migration execution belongs in this canary.

Before a call, freeze the integrated production source/dependency hashes, actual fixture SQL and cleanup scope, nonsecret configuration commitment and exact DEV identity. Validate actual SQL with its real parser in the separate DB lane. A synthetic runtime fixture must satisfy current owned runtime, approved person profile/calibration, active capability, grant/source eligibility and owned dialogue/log relationships. Never borrow owner data or create production consent. A direct production-function canary proves no deployed HTTP authentication or UI behavior.

## Production call chain

1. Seed explicitly synthetic owned runtime and complete dialogue/log records in the DEV fixture scope. Seeded historical responses are fixture data, never label them actual Azure responses. Use recordOwnedTurnFeedback in api/_replica-feedback.js to create feedback and encrypt corrections with the real helper. Do not insert invented candidate output or ready evaluation states.
2. readOwnedFeedbackDatasetReview then buildOwnedFeedbackDataset in api/_replica-feedback-dataset.js with the exact reviewed source_set_hash. Require production readiness with no blockers:12 independent sessions,6+ train sessions,2+ development,2+ test,30+ train preference pairs,20+ development examples,30 test examples,10+ heldout positives and all five dimensions covered. Session assignments are locked by the production deterministic builder, never forged in SQL. A compact fixture can first compute the real12-session assignment, then allocate four preference examples to each of8 train sessions,10 exact examples to each of2 development sessions and15 exact examples to each of2 test sessions, yielding82 rows. Confirm the actual builder returns those assignments/counts before seeding; do not assume random UUIDs force the expected split.
3. Construct createAzureCorrectionStrategyAdapter from api/_correction/providers/azure-foundry.js with strict revisionBinding and invoke runOwnedCorrectionCandidate in api/_replica-correction-candidate.js exactly once. Input replica_id,dataset_id,expected_source_set_hash. Production HTTP caller is api/replica-correction-candidate.js and now forwards the expected-model setting to the adapter. Require state draft, active_changed false, hash-valid artifact/build_manifest, matching provider_identity and binding. Abstention or any failure ends this canary without retry or fabricated draft.
4. Construct createProductionComparisonGenerator from api/_dialogue/registry.js. Do not use createProductionDialogueGenerator, whose legacy default is unbound. Invoke startOwnedMaterialization in api/_replica-candidate-materializer.js once with replica_id,dataset_id,candidate_id,expected_source_set_hash. HTTP caller is api/replica-candidate-materialize.js. This creates the complete30-heldout/60-item job without provider dispatch.
5. Invoke advanceOwnedMaterialization exactly twice, sequentially, and only while previous durable status is preparing and completed count matches0 then1. The first role is baseline, second candidate for the same feedback question. Before the second call independently inspect the first item's completed state, role/sequence and settled reservation. Use no UI auto-advance loop. Stop at completed2,total60,state preparing. Do not invoke finishPackage, qualification, owner vote, activation, publication or rollback activation. This is one isolated-question pair, not a blinded completed evaluation or qualified learning loop.

## Configuration, dispatch and money

Azure endpoint https://raghavsharma1729-compan-resource.services.ai.azure.com; deployment AZURE_FOUNDRY_DIALOGUE_MODEL=gpt-4.1-mini; AZURE_FOUNDRY_EXPECTED_RESPONSE_MODEL=gpt-4.1-mini-2025-04-14; AZURE_CORRECTION_BASE_MODEL_COMMITMENT=5c3f362c981523d2d6fdde5876dd12a041352a47cfe761c0bde034d7854dd5d5. The commitment is time-scoped observed metadata, not immutable weights. Historical fingerprint fp_8d7b600b57 is contextual evidence; do not require it to persist forever. Require each response's valid model/fingerprint and equality of both comparison-role receipts. Correction extraction may have a different fingerprint from the later comparison, and must retain its own provenance.

Protected settings: AZURE_FOUNDRY_API_KEY, actual DEV DB bootstrap, REPLICA_FEEDBACK_KEK_ID/B64, REPLICA_EVAL_KEK_ID/B64. Never print values or executable config content. Existing meter settings AZURE_REPLICA_BUDGET_ID, AZURE_REPLICA_APP_BUDGET_USD, AZURE_FOUNDRY_INPUT_USD_PER_MTOKENS and AZURE_FOUNDRY_OUTPUT_USD_PER_MTOKENS must exactly match the existing text authorization/ledger. Last parent report: limit1000000microUSD,spent142278,reserved0; this is stale until fresh readback. Do not create a new budget or raise a limit. Provider meter ensureProviderBudget can insert an absent budget; launcher must require that exact existing row before invoking production functions.

At most3 actual POSTs: correction max1200 output tokens, baseline max700,candidate max700 (2600 aggregate output cap). Input reservation must use production conservativeTokenEstimate and tokenReservationMicrousd on the exact correction messages plus response-format schema and each comparison messages plus DIALOGUE_OUTPUT_SCHEMA. Record all3 projected maxima and remaining allowance before dispatch. Parent must review a concrete per-canary cap after pure fixture prompt construction. No blanket authorization of the remaining budget is inferred here.

Use the production reserve/begin/settle/uncertain helpers. Keep durable ledger changes outside any transaction that could be rolled back after external spend. No mock q, mock fetch, supplied proposal, or replacement billing path in an actual launcher. One-use durable intent, exact pins, allowed endpoint/POST-count ceiling, hard process watchdog, bounded error projection and terminal receipt are required. Never auto-replay a started or ambiguous job. Missing/mismatched identity with measured usage settles the incurred tokens and refuses the draft/pair; a transport uncertainty retains reservation/reconcile_required. A stopped process is not zero spend. Never erase/release an uncertain ledger row as fixture cleanup.

## Receipt and cleanup

Allow only typed metadata: run/source hashes, UTC start/end, synthetic fixture IDs, exact DEV identity commitment, provider-call count, job/item state, model/fingerprint/binding receipt hashes, artifact/manifest/core/prompt/output hashes, token usage, reservation IDs and before/after ledger totals. No request/response bodies, preferred/rejected text, source URLs, credentials, connection strings, headers or raw exceptions. Provider output lives only in production private artifact/encrypted output storage. Persist correction identity in its hash-bound build_manifest; an abstained/failed correction has no usable candidate manifest and cannot proceed.

Independently read back candidate draft and unchanged active runtime, both items' encrypted outputs and provider identity, same heldout question/session, exact baseline/candidate core commitments (156), settled measured costs or explicit unresolved hold, and remaining58 pending items. Label success only: actual Azure correction draft and one private baseline/candidate pair,2/60 outputs, no qualification or human preference evidence. Full source/SQL correctness, deployed route, safety qualification, owner likeness and product quality remain separate evidence.

Fixture retention/retirement must preserve all spend/unknown-call evidence and source hashes. Freeze reviewed fixture-only erasure SQL or production erasure caller before execution; do not delete by broad prefix or claim production erasure from a rollback of uncommitted data. Any unresolved provider outcome ends with held funds and explicit root handoff.
