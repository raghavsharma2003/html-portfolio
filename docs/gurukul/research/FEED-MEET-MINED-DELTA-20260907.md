# Feed return: mined-state delta

2026-09-07. Root native checkpoint24 Feed receipt completed16:44:42.647Z with file status mined, readiness state ready and can_ask true. This is actual retained source evidence, not a synthetic-state assumption. The source shortcut introduced in checkpoint25 incorrectly required extracted only, although backend private rehearsal explicitly accepts extracted and mined. Original frozen candidate source hash b66074d371b3ce85a42b660a2a883350f460a2f819c50679f1b6f914d6694438 is the exact negative.

Decision: widen only the CTA status predicate to [extracted,mined]. Every file-format, own-context, owner-authorship, nonempty extraction, optional caller, busy/loading and current-scope guard remains identical. Existing View phrases control remains beside the shortcut. No readiness, source authorization, mining, model or mutation behavior changes. Reverse if mined status can permit a formerly ineligible source or if exact readiness is skipped.

Executed:15 offline groups render the actual TypeScript-AST-extracted JSX CTA branch using ReactDOMServer. The old source reconstructed by replacing the one predicate is SHA-verified against the original frozen candidate. It renders no button for the actual mined state. Current extracted/mined render the button; refused/routed/pending/extracting, wrong-kind, unknown/reference attribution, wrong consent, empty extraction and image remain absent; optional caller absent is absent; busy/loading remain disabled. Artifact scratchpad/feed-meet-mined-cta.log. This is actual branch rendering, not an entire mounted-component test or SQL proof.

Prepared, not run during release24: the existing full Studio mounted runner now adds390/1440 mined round-trip cases (View phrases still present, question/ID retained, exact authenticated readiness GET, empty attestations, zero automatic mutations) and routed-state exclusion, alongside existing10 controls. Its default count is therefore13. node --check passes. Final mounted/forced TypeScript checks await root timing clearance.

Rejection: the original extracted-only fixture falsely represented all successful Feed outputs; it missed the native mined state. Do not collapse mined into extracted or hide this seam as fixture-only. Original10-case and29-case results remain valid only within their recorded pre-repair scope, not acceptance of the native mined journey.

Apply as a small delta after FEED-MEET-RETURN-FREEZE-20260907.json: replace ContextLockerPanel.tsx and evals/feed-meet-return/run.mjs; add mined-cta.mjs and this report. Leave CloneExperience, PrivateTextRehearsal and callbacks unchanged. Root can register mined-cta.mjs as a node-only suite or invoke it from the existing source callback lane. No integration edits, actual network/model/DB call, build or browser run occurred in this delta.
