# Review malformed private draft fields without losing them

Private draft saving intentionally retains invalid and incomplete field values, returning validation errors as data. The previous nullish display defaults handled missing fields but still crashed both actual editors for saved strings in array fields, null analogy rows and object doubt steps. Checkpoint23's missing-field repair remains valid; this is a separate checkpoint24 extension for present malformed values.

The shared display projection now checks each rendered string, enum and collection shape. It supplies safe control values and a set of fields needing review. Raw draft JSON remains the editing and saving state. Named invalid values appear beside the appropriate controls or read-only preview, with English/Hindi notices. Missing fields keep the previous blank/unset behavior.

Malformed chapter and doubt lists remain untouched and disabled until an explicit replacement action starts a local empty list. The owner must still save to persist it. A replacement moves focus synchronously into the newly usable control, before any later interaction. Normal check/add/remove callbacks also inspect the current raw state before operating on a list. Changing subject preserves a malformed chapter list instead of discarding it. Untouched and unrelated edits preserve malformed and unknown JSON values. No publishing, consent, epoch, compiler, server or SQL behavior changes.

Native validation on2026-09-07:13 controls passed using actual saveOwnedTeacherSheetDraft, generated validation and React renderToString with synthetic injected SQL. Five exact-saveable malformed inputs reproduce ten failures in the exact old components/projection, while current components show review states. Additional scalar/read-only cases and missing/well-shaped fields pass; default database and fetch counters remain zero. The initial source-pinned diagnosis is ROOT scratchpad/expert-tools/private-draft-malformed-render-probe-20260907.json. This is not SQL, authentication, browser lifecycle or model evidence.

After the parent release23 heavy hold cleared, one full mounted48 batch passed at390/1440, retaining all26 prior controls plus22 malformed-data controls. Forced tsc -b --force passed. The actual copy gate passed7 scopes with21 negative controls, and context graph2329nodes/2376edges passed. Artifact: scratchpad/private-draft-editor/1788797334954/result.json. It covers raw preservation, explicit replacement and subsequent list interactions, scalar edits, synchronous focus and Hindi states. No full release ran in this isolate.

Reversal condition: reject any repair that serializes projection defaults, silently filters/drops malformed rows on an untouched or unrelated save, presents malformed saved values as confirmed empty data, or creates a new authority or publication path.

Merge note: root staging24 independently changes publication controls in both editor consumers. This isolate stays based on0a3b2d26. Apply its bounded source delta with a three-way merge, preserving the publication work; do not overwrite either editor with this branch's full file. Native/mounted results above describe this isolated delta, not the still-unmeasured combined publication tree.
