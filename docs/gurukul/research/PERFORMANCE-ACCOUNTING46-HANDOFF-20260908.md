# Performance accounting46: reviewed source and12 controls passed

Separate additive slice after visual42 f61a384e. No product font/layout optimization, browser run, build, waits, throttles or budget edits. Historical release39 failures and diagnostic40 incomplete traces remain intact.

The old gate kept live CDP byte counters through asynchronous profile/diagnostic/cleanup work. Diagnostic40 run1 actually recorded pending fonts at the settled observation yet later returned84148 font bytes after trace drain. Default gate shares the mutable-counter pattern but its actual drift magnitude is unknown. The new accounting helper snapshots bytes, request count and Hindi subset synchronously after readSettledPerformance resolves, before another await. Both receipt and nested byte object are frozen. Node timestamp and explicit boundary name mean events received by Node at that return point, not an atomic cross-process browser instant or proof pending fonts completed. Existing later resource/renderBlocking snapshots remain later observations; the patch does not pretend to synchronize those clocks.

Hindi41's real signed-out AuthGate now loads hiAuthCopy. The old instrumentation-only exemption therefore undercounts real JS. Completed Hindi chunk bytes now count once in category/total and separately as a diagnostic subset. Existing first-paint import timing workload stays unchanged. This may reveal a real JS budget failure; thresholds are unchanged.

After root source review,12 light controls passed in a0.884s command (2026-09-08; no browser): immutable late-font/late-request/late-Hindi capture, old shared-object negative, diagnostic subset without double charging, ordinary target accounting and real gate caller/returned metric wiring. Copy law passed7 scopes/21 negative controls in4.744s. Durations include shell overhead; no precise UTC was captured for these short commands. Prerequisite fixture includes the imported helper and eval registry only adds the suite without deleting incumbent entries. Broader tests/TypeScript/browser remain with integration45's reserved lane.

Detailed source evidence and rejected speculative optimizations: ../expert-tools/PERFORMANCE46-SOURCE-FOLLOWUP-20260908.md. Native597ms shaping in diagnostic40 has no retained DOM/font identity and occurred on the old English-only AuthGate; it does not justify changing current Hindi typography. New visible-interface checkVisibility can flush style but was absent from40 and cannot explain40's tasks. Require attributable current-source evidence before optimizing it; never restore hidden-logo readiness.

Reverse/narrow if the snapshot fails to resist later event mutation, accounting double counts or the real Hindi request is excluded. No pass/performance improvement claim until actual controls and unchanged integrated release checks run.
