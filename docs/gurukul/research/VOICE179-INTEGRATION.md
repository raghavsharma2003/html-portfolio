# Voice179: source-only allocation integration

Base df70e3e9; forward-port of reviewed 63e0c319/30bef40c, not a new ledger.

Actual caller: voice-preview owner authorization -> local protection constructors -> allocation readiness -> provider single runTransaction -> one147 reservation + atomic158 lifecycle/child plan -> signed broker child callback CAS -> absolute dispatch deadline -> private runtime -> existing protected result storage.

The provider cannot call standalone GPU readiness. At most30 readiness attempts and one continuous synthesis share one parent window. Unknown cost remains held. The original df70 PREVIEW_FENCE is byte-identical after extraction; current source-storage writer and protection-before-wake handling remain. New synthetic production-default negative proves zero broker/runtime/synthesis/reference reads without allocation, including status.

Operationally DISABLED. Migration158 is source-only, requires real SQL parser/concurrency/revocation proofs. Requires an isolated existing private App, exact image/revision/configuration/template/contract pins, matching broker/runtime origins, policy bound to owner/reference/exact Hindi text,147 budget approval, live independent supervisor lease, broker callback deployment and scoped Container App managed identity. Local174 has no Container App identity endpoint and cannot enable this via environment values alone. Initial isolated App creation can allocate GPU and is NOT covered by this existing controller. Bootstrap accounting and independently observed cleanup are required before any deployment. Shared production App remains ineligible; no minReplicas change.

Environment names: AZURE_VOICE_APP_ENABLED, AZURE_VOICE_APP_SUPERVISOR_ENABLED, AZURE_VOICE_APP_PLAN_JSON, AZURE_VOICE_APP_POLICY_JSON, AZURE_VOICE_APP_APPROVAL_SHA256, AZURE_VOICE_APP_BUDGET_ID, optional AZURE_VOICE_APP_IDENTITY_CLIENT_ID, platform IDENTITY_ENDPOINT/IDENTITY_HEADER; matching AZURE_OPEN_VOICE_ORIGIN/OPEN_VOICE_HMAC_SECRET; broker OPEN_VOICE_BROKER_ORIGIN/OPEN_VOICE_ALLOCATION_ORIGIN and existing private runtime origin. Existing protection/storage bindings remain mandatory. Preserve138600microUSD unknown hold. No cost-limit increase made.

Offline validation 2026-09-09: existing outer-allocation and callback scripts passed; controller22; open-voice78; panel135. Direct Python broker tests blocked because system Python lacks fastapi; no installation. Python source compilation is covered by open-voice test. No SQL, browser, provider, cloud or full release run. Initial panel test failed on its old exact no-argument constructor locator; updated it to require allocation injection while retaining the protection-constructor assertion. Product client timeout remains its incumbent value; before any live420second canary align it with existing53 client timing or run a reviewed operator caller. This source freeze does not claim full end-to-end operational readiness.

## Successor180 (2026-09-09)

Frozen458 remains unchanged. Broker11 offline tests now pass on that exact source using an ignored isolated venv, existing project pins fastapi0.116.1/httpx0.28.1/uvicorn0.35.0. System Python missing-fastapi failure remains recorded. An overbroad socket guard initially blocked Windows asyncio internal socketpair; the corrected guard permits loopback only and forbids remote connects,11passed. No global installation or runtime external network.

Restored existing53 client450000ms deadline above server420000ms and explicit nonretryable platform allocation failure. No lifecycle, budget or consent activation. Independent review found global due scans could let foreign rows fill LIMIT10 and starve/degrade the configured controller. Successor scopes due SQL by exact app/revision/contract before LIMIT, orders by deadline/window, binds actual runtime.plan and defensively rejects foreign result rows. Mixed scopes and stale-query negative controls pass; no actual SQL parser/race proof claimed. Previous app/revision/contract scopes still require their own retained valid controller/recovery job until terminal observation and accounting reconciliation. Deploying a successor does not authorize abandoning or mutating them; scope filtering is not a global cleanup guarantee. No rows deleted or cost holds released.

Validation: scope/caller/timing script passed, panel135, copy21negativecontrols. Copy initially lacked TypeScript in the new isolate; copied existing product175 TypeScript dependency locally and reran successfully. No browser/fullrelease or further provider test repeats. Independent review required before integrating/enabling.
