# Private guidance recovery after reload

The combined complete-only action disappeared after a successful private correction changed the rehearsal epoch. If the save response was lost and the owner reloaded the persisted request, the result became blocked and there was no caller for the supported current-guidance read.

The result store now adds `can_review_teaching:true` only for an actually stored complete request whose receipt and current source pass validation and whose snapshot fails with `rehearsal_inputs_changed`. No SQL changed. The public validator accepts that marker only on the exact blocked response. Settled requests may show **Check saved guidance**, which opens an explicitly read-only review. It cannot save, clear, automatically retry a POST or generate; an unexpected editable GET is rejected. Current guidance is not presented as proof that this particular save committed. A fresh question still requires explicit input and attestations.

Validation on2026-09-07 in the isolated recovery tree: focused mounted14 then full mounted40 groups (the29 incumbent plus11 new) passed at390/1440. Both lost-save and explicit-clear reload paths passed, as did the executed old complete-only negative, ineligible states, malformed editable recovery and delayed account/replica/request responses. Seven actual-store controls and16 incumbent store controls passed with synthetic injected rows and actual crypto/canonical evidence. Forced TypeScript and context graph2300nodes/2367edges passed. These are offline and localhost controls, not SQL/auth/model or owner-quality acceptance; no full release ran here.

Full mounted artifact: `scratchpad/private-teaching-refinement/1788795641702/result.json`. The exact executed old conditional source is retained alongside it as `old-complete-only-result.tsx`. The initial fixture assertion expected blocked for every bad evidence/account branch; existing named503 refusals required an honest fixture correction, with no product policy change.

Merge only the seven changed production/test files in the ROOT handoff manifest over the reviewed combined prerequisites. Add `private-refinement-recovery: private-refinement-recovery.mjs` to the root-owned eval registry. Existing private-teaching-refinement-ui already runs all40. Append the provided three context entries and graph nodes/edges; do not replace the combined context with this isolate's older base. Parent's earlier11 real SQL groups with two blocking witnesses remain separate evidence for the unchanged refinement CAS.
