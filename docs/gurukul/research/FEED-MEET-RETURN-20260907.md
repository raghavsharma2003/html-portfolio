# Feed to Meet source return (checkpoint25 candidate)

Base: checkpoint23 0a3b2d2608d64a4f9aebdafc690caf445f6b5889. Isolate: scratchpad/feed-meet-return, codex/feed-meet-return. No integration or checkpoint24 edits.

## Decision and scope

The actual owned, extracted text/Markdown/PDF/DOCX locker row can offer Test this source (Hindi: इस सामग्री से पूछें). This is optional host navigation, not source eligibility authority. It is offered only for the latest restored own-context, owner-attributed row. Recent upload receipts, attribution controls, removal and phrase review stay intact; the action appears in the refreshed saved row below receipts.

CloneExperience keeps the unsent question and chosen draft/context-item UUIDs only in its current in-memory return snapshot. Identity, token or replica change clears it synchronously; unmount clears it; old callbacks check current identity/token/replica and mounted status. Back to private test returns without replacing the selected source. The row action replaces only the context-item hint. No question, source text or token is added to URL, localStorage or sessionStorage. Existing authentication storage is unchanged.

PrivateTextRehearsal initializes from that snapshot and invokes its existing scoped readiness GET with the exact chosen IDs. No fallback source or automatic draft selection is introduced. A missing/removed source remains blocked. Attestations always start empty after returning. Save, ask, consent and inference remain separate existing explicit actions. No new draft editor, API, grant, runtime or publication behavior.

A current-workspace saved request suppresses the optional source shortcut: silently deleting its handle would evade the existing recovery/Prepare another question flow, while retaining it would deceptively show the old result as this source's test. The ordinary existing private-test entry remains available. Reverse this choice only with an explicit, independently verified request-resolution UX.

## Executed evidence

2026-09-07, actual built full Studio entry, normal CSS and synthetic loopback HTTP. No real auth, DB, storage, model or identity operations.

- Before production edits, checkpoint23 full-shell negative executed: opening source editor discards the unsent question and both chosen IDs; no source shortcut exists. Artifact scratchpad/feed-meet-return/1788798647619/result.json and old-source-390.png; log scratchpad/feed-meet-baseline.log.
- First new batch: 29 mounted groups passed, including 9 new handoff groups and 20 existing private rehearsal cases. Artifact 1788799004394/result.json, log feed-meet-mounted.log. This preceded the final identity/unmount defense and saved-handle suppression.
- Final bounded confirmation completed 2026-09-07T16:40:58.720Z: 10 new mounted groups passed on the exact final three production hashes. Artifact 1788799227800/result.json, log feed-meet-final-controls.log. Contains 390 Hindi/1440 English, Enter/Space and pointer, preserved question/IDs, fresh scoped readiness, empty attestations, no automatic mutation, reference/unknown/refused/image exclusion, removed-source refusal, saved-request preservation and delayed-readiness refusal. Both widths have no document horizontal overflow; source buttons are at least44px high. Reduced motion was requested at390; no new motion code was added.
- 12 actual callback/render-scope groups execute TypeScript AST-extracted production callbacks and scope reset, including identity/token/replica/unmount, foreign/malformed row IDs, switch-back clearing and exact old callback negative. This proves callback behavior, not a mounted auth-transition journey. Log feed-meet-callbacks.log.
- Forced TypeScript passed before the final saved-handle conditional; final built browser compilation passed. Root should run final forced TypeScript after the timing hold. No full release or performance claim.

The final production source hashes are recorded in the final browser result and freeze manifest. The runner's only subsequent change makes the already-executed bounded10 cases the default, with --incumbents explicitly adding the existing20; this avoids doubling those browser cases in the release registry. --baseline replaces only the actual three production modules with exact git checkpoint23 sources and executes the retained old full-shell negative. --source-only preserves its existing client/navigation controls. Callbacks run separately via callbacks.mjs.

## Visual review and limitations

Inspected source-390.png, source-1440.png and returned-390.png/returned-1440.png. The added row button uses incumbent wrapping action styles; no new palette, font or CSS. The returned390 screenshot captured inherited scene opacity during transition, so it is not contrast acceptance. Existing locker link fields and older copy remain outside this narrow change. No claim of native Hindi quality, full accessibility, voice likeness or overall journey completion.

## Rejections and failures retained

The old indirect Back to choices route loses local state; a URL or web-storage copy of the question was rejected in favor of ephemeral parent ownership. Automatic selection authority, save, attribution, consent or asking on navigation were rejected. A saved-handle shortcut was rejected because it would reopen the wrong result or require silently erasing recovery state.

Initial test-file generation hit Windows cp1252 encoding and a missing scratchpad output directory before any test executed. Initial inline JSX patch omitted the fragment around two siblings; tsc reported TS1005/TS1382 then TS17008/TS1381 until the fragment closed correctly. Those were implementation syntax failures, not product runtime evidence; the final compiler/build controls passed. Initial context check reported three missing graph nodes; they were then indexed and the original failure log retained. Earlier tsc error output was overwritten by the successful tsc log, so the tool transcript is the retained exact error evidence.

## Handoff

Production files: src/studio/CloneExperience.tsx, ContextLockerPanel.tsx, PrivateTextRehearsal.tsx. Tests: evals/feed-meet-return/run.mjs and callbacks.mjs; baseline.txt retains the originally executed old test body. Suggest registry entries feed-meet-return-ui -> feed-meet-return/run.mjs (same browser/Vite resource treatment as private-text-rehearsal-ui), feed-meet-return-callbacks -> feed-meet-return/callbacks.mjs (node only). Registry intentionally unedited for root merge.

Merge against stated23 base, preserving later checkpoint24 deltas. Do not copy old context files wholesale: append this isolate's named entries. Reversal requires a reproduced scope leak, lost unsent question, wrong-source selected response, implicit mutation, recovery-handle loss or equivalent simpler safe UX.
