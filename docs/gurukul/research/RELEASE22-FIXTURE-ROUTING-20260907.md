# Release22 fixture routing repair, 2026-09-07

Isolated source base43230e5e. Publication implementation is paused and its two API edits are excluded from this handoff. No change to the four runtime modules under test: `_context-locker.js`, `_replica-source-erasure.js`, `_review-queue.js` and `_room-surface.js`.

Root's retained release22 log exposed four failures. `context-proposal-erasure` and `contextlocker` expected a direct authorship UPDATE, but actual re-mine now begins with a source lock and owned private-text epoch update. `reviewqueue` expected `with authorized`, but the statement now begins with `authority` and advances the epoch before the existing decision gates. `room-flags` expected `with existing`, but the real never-rule insert now begins with `private_text_fence`; its wrapper fell through to the generic Room fake and returned no row, so the feature reported a null rule and three assertions failed. These are observed fixture routing mismatches, not a reproduced production SQL defect. No SQL execution or concurrency claim follows from fixing a fake.

The five fixture/eval files route the current statement shapes, assert retained source/owner/epoch fence clauses, and model authorized epoch advancement. Flag writes of an unrecognized shape now fail loudly rather than falling through to a plausible empty base reply. Existing assertions remain; new controls reject removed epoch/source ownership clauses, keep another owner's attribution/epoch/rules unchanged, and demonstrate that unrecognized never-rule writes cannot be silently accepted. The re-mine erasure negative still stops at the real insert after removal and observes no recreated run.

Results on2026-09-07, Node24.13.0, synthetic offline config with global fetch refused: context proposal erasure9groups, contextlocker98assertions, reviewqueue194assertions, room-flags45assertions, all pass. Baseline release22 had6/9erasuregroups then fixture error, contextlocker fixture error, reviewqueue fixture error, and room-flags37pass/3fail. Original root logs remain at expert-integration/scratchpad/release-logs/2026-09-07T14-57-44-255Z-5664/eval-suite.stdout.log. Local final logs remain in this isolate's scratchpad. An initial isolated rerun could not import ignored `_config.js`; a newly generated synthetic-empty config resolved that environment prerequisite, without reading or copying keys.

The reviewqueue log's historical statement that migration074 had never been applied was replaced by a timeless limitation: this suite neither migrates nor EXPLAINs SQL. Root's prior13actual development SQL groups remain separate evidence and do not imply all source-lock behavior has been proved.

Reversal: reject this fixture change if a real captured statement no longer has the asserted authority/epoch semantics or actual database execution contradicts the modeled row effects. Keep unmatched writes failing; do not weaken runtime gates or infer production acceptance from the offline green result. No DB, provider, model, browser, deployment or new build.
