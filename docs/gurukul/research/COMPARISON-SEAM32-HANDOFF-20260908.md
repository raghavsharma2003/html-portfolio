# Fresh comparison selection seam 32

This candidate connects the prepared-purpose pipeline to private comparison selection and the real verification Journey. It combines frozen145 (97d02fec), preparation146 (c5385f85), and allocation147 plus its completed-reader follow-up (6dbb6d2a). It does not activate a GPU, complete identity verification, or establish voice likeness.

The owner can open Add a comparison recording, explicitly authorize its processing, upload through the existing authenticated create/signed Azure PUT/finalize contract, recover the same preparation ID, or withdraw. Preparation availability remains a platform blocker. Upload permissions mount only after opening that action. Existing prepared recordings remain reviewable through separate private-use permission, verified-byte audition and explicit owner assertion.

Completed146 evidence is discovered independently of ordinary voice primary. It must retain exact source, owner, job, preparation receipt, completion manifest, pinned evidence and current consent bindings. A selected145 reference UUID becomes the dedicated selection identity in the modern descriptor/challenge. The immutable use receipt keeps a null ordinary selection identity. Withdrawal, missing or changed completion, false purpose consent and expired authority refuse further use. Existing ordinary evidence keeps its prior reviewed path.

The Journey reads the server's selected descriptor after selection/status/withdrawal. A fresh private selection can reach ID and liveness checks with recording/storage permission, without first granting transcription/training. Identity completion still returns to ordinary recording, transcription and build permissions before a voice build. Nothing mutates the ordinary primary pointer on the private path.

## Evidence and boundaries

On 2026-09-08: 23 synthetic seam controls, 31 existing Journey controls, 15 actual-client synthetic transport controls, 41 existing authority controls, 19 existing comparison controls, eight SQL preparation guards, 15 allocation controls, and 28 preparation groups passed. TypeScript passed after source integration; final copy and mounted results are logged below. Private npm ci installed158packages in39seconds. Unlike the frozen145 verification, this worktree owns its dependencies.

The parent mounted fixture adds fresh prepared-source groups over actual Journey/component/client/HTTP-store callers with synthetic SQL/PCM. The standalone upload fixture exercises actual UI/client with synthetic HTTP and Azure upload responses. Their final browser executions passed34parent and6uploader groups after root released the browser lane. No fixture is an Azure, SQL, independent biometric or human listening proof.

The 147 first manifest failed preflight because its declared commit and test hash disagreed. No source was imported until V2's canonical15file pins matched6dbb6d2a. Registry, schema, context, owner export and erasure were combined by explicit union. The completed-preparation reader now consumes response_recorded dispatch state, preserving accounting_pending rather than claiming invoiced cost. Production remains unavailable without the finite allocation controller.

Independent read-only review identified different latest-consent tie-breaks in145 and146. Fresh discovery now matches the immutable still-active consent IDs, while ordinary discovery retains its prior latest-choice behavior. The test scope does not prove PostgreSQL ordering or lock behavior.

## Required before activation

Apply145/146/147 only to the exact isolated development database through a reviewed guarded launcher, then EXPLAIN the changed actual query shapes and exercise scoped lifecycle/concurrency with durable exact-ID cleanup. Add fresh prepared fixtures to the ordinary145 live runner: valid stage dispatch receipts, source/job/preparation FKs, completion tampering, equal-time multiple consent rows, withdrawal versus selection/issue/load, no fallback to ordinary authority, and no ordinary primary pointer/epoch mutation. The live runner's source-pin inventory now includes the new imported authority dependencies and migrations; its current synthetic runtime seed still covers the ordinary-source branch only.

Actual finite-controller allocation enforcement, live owner/provider upload, processing, calibrated same-capture identity, and multilingual owner likeness remain unfinished and must be measured separately. No migration, cloud write, provider call, deployment or release promotion occurred here.

Preliminary source freeze: root authorized freezing while both mounted suites remain pending. Final source checks include57export, TypeScript, copy7scopes21negatives and resource classification for both browser suites. Exact6671d632 copy replacements were applied in five files; the release31 readiness fixture contract changes remain a separate integration requirement. Run `node evals/comparison-reference/mounted.mjs` and `node evals/comparison-preparation-ui/mounted.mjs` after root browser clearance, then record actual artifacts and freeze a verification follow-up.


## Final mounted verification

Both suites completed sequentially after root browser clearance. Final parent receipt `scratchpad/comparison-reference-mounted/1788849093326/result.json` passed34groups, 12recorded source hashes matching after the run and errors empty; final uploader receipt `scratchpad/comparison-preparation-ui/1788849077454/receipt.json` records6groups and its process exited0. Both390/1440 were exercised. Actual screenshot inspection found and repaired new-upload instructions remaining beside prepared status; the new assertion checks those notices disappear for saved material. The original passing-but-confusing uploader receipt1788849012056 and parent1788848949548 remain preserved. This supersedes the preliminary browser-pending instruction above, without adding any realSQL/provider/biometric or voice-quality claim.
