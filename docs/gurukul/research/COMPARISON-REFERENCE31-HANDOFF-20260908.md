# Private comparison selection, candidate 31

This isolated change connects an owner's explicit private comparison permission to existing completed voice evidence. It does not complete identity verification, generate voice, or authorize training. Migration 145 is prepared and mirrored, but has not been applied or parsed against a database in this workstream.

The owner can choose a prepared recording version, grant its three specific uses, privately audition verified bytes, explicitly confirm the voice, and withdraw the choice. The HTTP endpoint returns private no-store audio instead of a reusable storage URL. Authority is checked before and after private reads; selection uses the existing replica authority epoch. Exact receipt replay and unknown-request withdrawal preserve uncertain network outcomes without inventing successful consent. The issuer consumes immutable evidence and a selected purpose receipt; ordinary reviewed authority remains a separate branch.

The actual Studio caller carries authenticated owner identity into the mounted Journey. Account, token, source and lifecycle changes discard pending UI results. Saved state contains only an account/replica-scoped opaque request handle. No permission controls appear when compatible existing evidence is unavailable. Owner export and full owner erasure include the new table, whose source/artifact foreign keys cascade.

## Verification on 2026-09-08

- 19 backend/HTTP controls passed using an explicit synthetic SQL dispatcher and private PCM bytes, including abort, invalid ranges, evidence replacement, pagination, withdrawal and issuer consumption.
- Eight SQL preparation guards passed. Nine actual query shapes and a ten-group injected development lifecycle runner are prepared; no SQL execution occurred.
- 41 incumbent modern authority controls passed.
- 28 actual Chromium mounted groups passed at 390 and 1440 pixels. Receipt: `scratchpad/comparison-reference-mounted/1788844457582/result.json`. The pinned prior Journey is a retained negative control. Actual handler/store callers are used with synthetic SQL, not a real database or provider.
- TypeScript, copy law (seven scopes, 21 negative controls), and creator export (57 controls) passed. These tests used the existing dependency junction to expert-29-release; this is not a private-install full release verification.

All three new suites are registered separately in `evals/run.mjs`, so browser classification can see the actual mounted suite. No full release, cloud write, migration, real listening evaluation or deployment was performed. Frozen base is 955ec825, incorporating the five selected-comparison UI prerequisites above authority 3462b1f6.

## Remaining integration

Frozen preparation 146 at c5385f85709ce60bbb30de080d618a777b2da680 creates a dedicated comparison-purpose upload. This 145 candidate discovers existing completed ordinary-source evidence and binds the existing primary selection identity. The fresh preparation purpose is deliberately excluded by ordinary primary selection. A dedicated preparation discovery and selection identity must connect 146 to this feature without permitting ordinary training or bypassing identity. Merge both features' owner-export/erasure entries, schema migrations and test registrations by union. Do not replace one version wholesale.

The modern capture producer remains unavailable without calibrated same-recording evidence. Permission, cosine similarity, recorded delivery and owner assertion are distinct from biometric acceptance. GPU allocation accounting and actual speaker/voice quality remain separate unfinished work.
