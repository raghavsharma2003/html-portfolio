# Browser eval165: creator and follower release failures

Base0e5392aea8da036d4e90c64b68fc82c0e774d45c. Test-only work; no API/UI/SQL/provider changes. Shared room fixture imported byte-for-byte from reviewed d2787827, SHA2566452776f101db06af4fa783feeeb7ff02a7d469d175c522fa67f305287074e34. Dependency lock matches sibling163, SHA2566cd79d2d783a6b56548ee323ef19ebdfa2f3c98f131557102ce81d9091ff40dc. Config generated with18 empty literal exports; no copied values.

## Creator

Original full-release failure:13groups passed, group14 (390/en/ops current declaration) timed out waiting for load at15s. DOM empty/interactive, no page errors;19requests with only1document complete in final navigation. This does not prove a product contrast defect or a host cause.

One quiet unchanged run exec87792 passed all32groups with original15s timeout and old-CSS negative controls. Receipt scratchpad/creator-cascade-order/1788888720033/result.json. Existing sibling163 dist was read through a local junction only for this check; no build or source mutation. Initial relative junction preparation failed before browser with ERR_MODULE_NOT_FOUND; creator-once.log retained. Actual run creator-once-prepared.log. Creator test unchanged.

## Follower

Original full-release error: room_memory_authority_unavailable after join. Reviewed shared room fixture supplied current follower epoch/revoke behavior. First actual private-build follower run exec44598 then completed62passed2failed in49684ms: reply offer null and missing offer card. Raw follower-once-prepared.log preserved.

Cause: room-doors fixture matched the new authority-first seven-argument meera_log insert with the legacy five-argument DM matcher, corrupting role/device/agent and returning no inserted row. Session-worked offer counting therefore saw no matching conversation. This was test data, not a UI defect.

New narrow matcher uses exact actual ROOM_MEMORY_LOG_SQL, rejects schema/argument drift, applies replica/Room/follower authority and epoch/consent/age/publication/lifecycle predicates, validates role/content, appends the correctly shaped log, and returns its id. The legacy matcher remains independently tested.22 focused controls cover successful both roles, wrong identity/epoch, withdrawn consent/age, unpublished/paused Room, revoked/purging/missing/mismatched replica, invalid content/role/shape, and legacy DM behavior. Controls execute before the existing follower journey, so they cannot become an unregistered orphan.

After the fixture changed, the single authorized run exec80217 passed22controls and64/64journey assertions in49402ms. Browser walk22142ms, fake WhatsApp139ms, fake Telegram83ms. Offer response/card now both pass. Original wrong-secret and other negative cases retained, no timeout/count weakened. Log scratchpad/follower-authority-repaired.log. English gate walk includes switching disclosure to Hindi and back; the optional full Hindi journey was not run.

Before follower builds, this isolate's dist junction was verified and removed by LiteralPath without recursion. Builds wrote a new private real dist. Existing163dist stayed untouched. Initial config-copy shape verification refused existing initializer shapes; config was instead generated as empty strings from AST export names. Earlier no-config import failed before browser and is preserved as follower-once.log. No fullrelease, realSQL, model-quality, cloud or production-deployment evidence here.

Reversal: reject the fixture if future SQL/authority changes diverge; exact statement matching fails closed for the new branch. Mocks prove fixture control flow, not SQL types, concurrency or real consent enforcement. Historical fullrelease failure remains a failed receipt despite these scoped passing checks.
