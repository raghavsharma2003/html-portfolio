# Advanced teaching sheet disclosures

Base: checkpoint24 `3db85f82f9491322a8db2a62556cf39be8234937`. Isolate: `codex/teacher-sheet-disclosures`.

The four teaching-choice cards remain editable and visible. Save now follows them directly. The six read-only draft fields are behind “Draft details (read only)” with an equivalent Hindi label; the existing mentor-boundary label opens its unchanged paragraph and helper text. Publication remains outside both disclosures, with its component and APIs unchanged.

Malformed fields retain the global notice and exact existing field/replacement notices. Each affected read-only disclosure opens initially and keeps a visible summary warning when manually closed. No guessed summaries, content extraction, inferred defaults, automatic saves or publication requests were added. Toggling native details is presentation state only.

## Evidence and limits

- Native17 passed using actual private-save/generated validation and React markup. This includes13 malformed incumbents and4 disclosure controls, with both exact checkpoint24 editor sources retained as structural negatives. Default DB and fetch counters are0.
- Forced TypeScript passed. Actual copy gate passed7 scopes/21 negatives. Mechanical detector returned[]; it does not establish usability or human acceptance.
- The single mounted18 batch passed on its first run at2026-09-07T17:02:33.569Z, after root cleared release24. Artifact `scratchpad/private-draft-editor/1788800541768/result.json` retains all18 checks, zero page errors, four old/current comparisons and ten screenshots. Both390/1440 editor lanes and Hindi passed keyboard disclosure, visible controls, exact read-only text, unchanged/unrelated saves, malformed warnings and explicit replacement/focus. No second browser pass was needed.
- The registered incumbent mounted suite retains its48 existing groups and adds these18 on its normal path; `--disclosures-only` ran only the new batch here. The full66 and publication40 were not rerun in this isolate. Parent full integration gate remains required.
- Existing root24 screenshot dimensions4291px mobile and2399px desktop are retained planning evidence. The new comparison uses the same complete synthetic fixture in exact old/current editors, not the root publication-review screenshot state: creator390 height4118 to2844, Save top3779.359375 to2407.765625; studio390 height4139 to2865, Save3800.359375 to2428.765625. Both1440 lanes: height2247 to1866, Save1935.015625 to1457.484375. Units are CSS pixels, one run per lane/width. Neither establishes task completion time or human cognitive-load improvement.
- No actual DB/auth/model work, authority/schema changes, whole Studio-shell proof or full release belongs to this slice.

## Verification command

Executed after root cleared the heavy gate:

```
node --import ./scratchpad/offline-hooks.mjs evals/private-draft-editor/run.mjs --disclosures-only
```

The ignored hook refuses secret-config imports and throws on any default DB use. It does not replace the mounted CSS or browser API wrapper.
