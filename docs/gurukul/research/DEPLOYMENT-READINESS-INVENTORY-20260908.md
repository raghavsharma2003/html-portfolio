# Deployment acceptance inventory

Observed 2026-09-07 18:44 UTC / 2026-09-08 00:14 IST. Read-only source and retained-evidence review; no runtime, browser, model, network, database or deployment calls. Only this report and its manifest were written in the existing isolate. Integration HEAD and staging25 base both read as `3db85f82f9491322a8db2a62556cf39be8234937`; staging contains uncommitted additions. This is a time-bounded inventory, not a claim that later release activity stopped.

**The nearest useful release is a private teacher text pilot. It is not yet an accepted general expert clone or public voice launch.** Current private editor and compiler support physics, chemistry and maths; the wider expert-first positioning exceeds the demonstrated vertical scope.

| Release scope | What exists and has evidence | Concrete acceptance still needed | Dependencies / deployment consequence |
|---|---|---|---|
| Private teacher text pilot | Actual owner-scoped upload, canonical extraction, minimal private draft, explicit one-question permission, Azure answer, encrypted saved result, GET/same-ID replay, withdrawal and source erasure. One fresh synthetic maths/Hinglish example correctly preserved checking 18 minutes before graphing 25, total 43, and unknown group size. One inference cost 539 microUSD; replay made no second call. Separate cleanup completed with 98 scoped table counts zero and auth absent. | Freeze the final merged first-use/Feed/editor/focus changes and pass the actual full release on those bytes. Close and retest the reported same-owner token-refresh race in pending create/list/select before declaring first use accepted. Restart the local backend against the accepted source and run the actual new-owner browser journey. Then verify the intended deployment's auth, schema, private encryption, exact Azure configuration, quota and lifecycle behavior with a bounded canary. Human teacher review is still needed for usefulness and correction burden. | Does not require voice/identity enrollment or public sharing. Existing native proof used synthetic auth/permissions, not an ordinary new customer's complete browser onboarding. Suitable first release scope after these conditions pass; no date established by this audit. |
| General expert private clone | Reusable private text architecture and owner editing exist. | Expand the deliberate three-domain validator/editor contract; evaluate fresh profession-specific material and refusals. Reliable personality/teaching adaptation, broad retrieval and useful private continuity are not established by one answer. Saved presentation-order correction failed on prose in all three factorial owner-guidance cases; the failed compiler-tail candidate remains withheld. | Do not market the teacher pilot as proven for any profession or as faithful personal cloning. Typed knowledge is an optional experiment, not a mandatory fact-editing onboarding step. |
| Ordinary public text sharing | Explicit same-sheet/version/hash publication review, final publish and readback now have real callers and nine scoped SQL proof groups. Existing historically eligible sheets can use that guarded path. | New owners lack a normal text-only agent/capability and genuine publication-permission producer. The existing agent writer belongs to full runtime activation; private drafts and rehearsal receipts do not create public authority. Need either genuine verified-principal provisioning or a separately defined account-published material-assistant capability, with explicit review, visitor admission, audience/quota, source/current-version checks, revocation and copied-data erasure. Prove owner-to-visitor flow and overlap behavior before exposing links. | Present public Room gates remain intact. A successful private test cannot authorize publication. The narrower account-published option must accurately disclose its provenance and cannot acquire verified-clone identity, voice or private memory authority. |
| Owner voice clone launch | Azure voice candidates, evidence/protection primitives, processing and historical syntheses exist. Primary-selection stale overwrite was repaired and has 29 scoped real SQL groups; this is not proof of speaker ownership. | Modern capture explicitly returns `liveness_verifier_unavailable`: the required composite operation is not implemented. Need version-bound actual recording/subject continuity and anti-spoof evidence, an exact primary-reference ownership receipt, and consumers at build/admission/settlement/cached delivery. Then run authorized current owner enrollment and protected synthesis, followed by held-out owner/native-listener likeness, naturalness, Hindi/Hinglish/English pronunciation, latency and cost evaluation. | No health check or config switch can manufacture composite capability. No owner likeness acceptance currently exists. Historical proxy scores and training runs cannot set a launch date or establish superiority. Keep unavailable capture and voice paths honestly blocked. |
| Website / web app release of the scoped pilot | A working codebase and older deployed website exist. Checkpoint24 completed 23/24 software gates; the locale scan false positive has a focused-tested staging fix. Staging25 adds reviewed first-use, Feed-to-Meet, editor and focus improvements. Action-focus merged receipt is dated 18:33:49 UTC; its source review found no actionable focus issue. | Final merged full release, including final auth-race repair; relevant target-environment SQL/schema/config checks; reviewed deploy artifact and target; authenticated post-deploy canary and rollback verification. Latest observed local backend remained checkpoint21 despite newer frontend/source work. Four older full-replica backup certificates remain a separately tracked erasure issue, not covered by the successful new source cleanup. | Deploy workflow triggers only `main` and `claude/ai-companion-app-rkt1lv`, not the current expert branch. A push alone is not evidence of deployment; choose and verify the actual intended release route. No push or production deployment was performed by this review. No native mobile-store release evidence was established. |

“When” is conditional, not a guessed calendar estimate: the private teacher pilot can be considered for deployment once its final combined release, actual first-use journey and target-environment canary pass. Public sharing depends on missing publication authority/callers. Voice additionally depends on missing verification/ownership implementation and human quality acceptance. These are different milestones; software gate completion alone does not complete the latter two.

## Evidence and exact reuse points

Paths below are relative to repository ROOT unless a source root is stated.

- Latest root `context/STATE.md` START HERE and staging25 context; retained checkpoint24 release status: `scratchpad/expert-tools/release24-locale25-context.json`. Historical “everything works/live” paragraphs do not override the current START HERE block.
- `scratchpad/expert-tools/native-feed-answer-run-checkpoint24.json` retains the actual answer and original cleanup refusal; `native-feed-answer-cleanup-checkpoint24-resume1.json` supplies the separate successful continuation. Do not rewrite the original run as an all-in-one pass.
- Staging source `scratchpad/expert-25-combined/src/studio/CloneExperience.tsx:1250` mounts the actual private rehearsal. `src/studio/PrivateTextRehearsal.tsx:251` requires the three minimum fields. Integration `src/engine/privateExpertRehearsal.ts:56` enforces physics/chemistry/maths.
- Publication inventory: `scratchpad/expert-tools/ORDINARY-TEXT-PUBLICATION-PROVISIONING-AUDIT-20260907.md`. Integration `api/_teacher-sheet-draft.js:282` names the missing binding/consent blockers; `api/_replica-runtime.js:372` gates activation and `:392` creates the agent. Existing adoption is a real post-activation caller, not an absent function to reinvent.
- Voice boundary: integration `api/_liveness/capture-readiness.js:6`; `scratchpad/expert-tools/MODERN-COMPOSITE-IMPLEMENTATION-PLAN-20260907.md`, `PRIMARY-VOICE-OWNERSHIP-BINDING-AUDIT-20260907.md`, and `VOICE-FRONTIER-NEXT-EXPERIMENT-20260907.md`. The older ownership audit's selection-CAS defect is superseded by `PRIMARY-SELECTION-CAS-HANDOFF-20260907.md`; its missing subject-to-recording authority remains distinct.
- `scratchpad/expert-tools/action-focus-merged25.json`, `focus-root-review25.json`, `first-use26-handoff-20260907.json`, and subsequent reviewer/owner correspondence distinguish focused synthetic checks from final merged acceptance. The same-owner refresh race is pending a separate owner's correction at this inventory cutoff.
- Integration `.github/workflows/deploy-web.yml:28` provides the exact branch trigger. No current production-state claim is inferred from this local file.

**Decision and rationale:** accept releases by capability, starting with the smallest useful private teacher scope, instead of assigning a misleading completion percentage to the entire clone platform. **Reversal:** hold or narrow that pilot if final first-use, private authority, erasure, billing, grounding or teacher review fails; expand only with actual caller, target-environment and user-quality evidence. Do not make private pilot acceptance wait for an unrelated voice benchmark, or treat a passing pilot as voice/publication acceptance.
