# Product voice allocation stages181/182

SOURCE ONLY. Not deployed, no migration applied, no GPU/provider call. Preserves canary392 and request-authority761 freezes.

181 removes deployment-configured owner/text and test preparation identity. beginOwnedVoicePreview supplies current owner/replica/source/generation plus intent/attempt/lease. Allocation admission checks actual DB generation text hash/language/reference, current consent/source/intent; atomic158 begin repeats it. Child consumption repeats authority and exact signed child body binding. Hindi and Hinglish use hi, English en; general multilingual Chatterbox remains the product arm. Qwen's separate English-only adapter is untouched.

182 replaces fixed pricing/budget literals with validated positive integers in reviewed immutable policy: rate_microusd_per_second, contingency_multiplier1..10, limit_microusd and per_allocation_cap_microusd. Calculated900second estimate must fit per-allocation and total cap.420second dispatch and900second cleanup bounds remain. Prices and their freshness still require deployment review; integers alone are not market-price evidence or hard invoice guarantees.

Each transaction requires an existing isolated, inactive revision and zero observed replicas.147 reserves before158 binds current authority and an activation intent. Durable activation claim occurs once and rechecks owner intent. After token acquisition, current deadline/window and independent supervisor lease are checked again before ARM POST. Only documented200 acknowledges activation, followed by exact revision readback; unexpected202/transport/DB uncertainty never retries or releases funds. Broker children require acknowledged activation. Cleanup deactivates only exact owned revision and continues observing; an unknown activation cannot become terminal merely from a later inactive readback. Operator reconciliation remains necessary for that uncertain case.

Azure activation200 contract: https://learn.microsoft.com/en-us/rest/api/resource-manager/containerapps/container-apps-revisions/activate-revision?view=rest-resource-manager-containerapps-2025-07-01

Repeat-use operational limits: controller can activate the same exact revision in a new window after observed shutdown.147 resource-exclusive unresolved windows still refuse another allocation until real accounting reconciliation; the synthetic second-controller-window test does not settle an invoice. Existing138600microUSD unknown hold remains untouched. Initial App provisioning/bootstrap is separate and is not implemented here. Existing shared production is ineligible. Old revision/contract supervisors must remain responsible until their own terminal and accounting recovery, not silently abandoned after configuration updates.

Validation2026-09-09:181 two-owner/text/language/reference/staleattempt/lease/withdrawn-authority controls; outer-window including exhausted/held refusal beforeactivation;182controller27 including two successive windows, claimed-onlyactivation, stale token deadline, unknownactivation nonterminal and variablepricing;panel135;supervisorscopecontrols. All offline/synthetic, not SQL parsing/races. First controller run24/26 failed because two incumbent fixtures accidentally changed active->inactive; restored their explicit active preconditions, then27/27 passed. Broker unchanged from392 (11passed previously), no duplicate rerun. No browser, fullrelease, migration, resource creation, cloudmutation or owneraudio access.

Next acceptance is independent source review, actual158 SQL parser/atomic race proof and exact production binding review. Missing managedidentity/supervisor/bootstrap/pricing evidence is operational work; these source tests cannot claim ordinary product voice works live or sounds like the owner.
