# Explicit action focus continuity, 2026-09-07

Candidate: codex/expert-action-focus from checkpoint24 3db85f82f9491322a8db2a62556cf39be8234937. Both owned components started as exact root combined25 bytes, captured with hashes in evals/action-focus/old-components.json. CloneExperience/first-use navigation is untouched.

Three explicit actions now preserve keyboard position when their initiating control disappears: writing attribution returns to the same source's available Test this source button (fallback locker heading); saved private draft returns to the selected-material heading; a completed private question goes to the answer heading. Headings are programmatically focusable and do not open a mobile keyboard. Native pointer/Enter actions are preserved. One-shot intents are canceled by subsequent pointer/key/input, focus movement, unmount and the existing keyed scope lifetime. A successful current response queues focus only after React commits, only if BODY still owns it and the original control is removed/disabled. Initial/readiness/restored-result reads never arm this behavior. No automatic save, question, grant, read retry or publication follows. No API or CSS change.

Evidence: one final batch58groups at390/1440, actual components and clients under StrictMode. Six exact-old BODY negatives and six corrected action controls; typing, away-then-body, stale token/replica/unmount, failure, Enter and delayed initial/restored reads. One POST per explicit action, no POST for initial reads; draft-save consent remains unchecked. Source hashes recorded in result. Forced TypeScript and copy7scopes/21negatives passed. Browser/server closed.

Artifacts: scratchpad/action-focus/1788803354581/result.json and six width/action screenshots; scratchpad/action-focus-batch-1.log, batch-2.log, batch-3.log; action-focus-tsc-final.log and action-focus-copy.log. First two failures are retained fixture defects (wrong response envelopes; sibling control under absolute private panel), repaired without product edits. The screenshots are functional component hosts with a synthetic sibling header and normal imported component/creator CSS, not full Studio-shell design acceptance. No device keyboard, real auth/SQL/provider or entire journey acceptance is claimed.

Implementation stays in the two assigned leaves. Their small local focus hooks intentionally avoid adding a shared bundle dependency across entry boundaries during this bounded repair. Existing operation/current guards, lost-response recovery and authoritative attestations remain in their actual callers.

Reversal: remove or revise if a later action steals user focus, opens input keyboard, replays work, or ignores existing scope guards. Root can register node evals/action-focus/run.mjs in the ordinary isolated browser lane: Vite write:false in memory, ephemeral port0, unique scratchpad outputs; never a shared dist writer. Merge context entries by ID; do not copy these older context files over combined25.
