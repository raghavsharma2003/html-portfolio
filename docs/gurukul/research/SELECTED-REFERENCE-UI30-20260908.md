# Selected recording comparison producer

Base: f4235c6809128d00f336fb73b127e75afe6493b2. This is frontend source and synthetic caller acceptance, not a calibrated verifier or identity result.

The existing CloneVerificationJourney permission step now reads the authenticated comparison descriptor and sends the typed modern issue body. The source/selection/hash and opaque comparison snapshot remain request bindings, never visible identifiers. The retained descriptor currently supplies a date and no filename or duration, so the UI shows the actual saved date without inventing either field. The eight required statements are individually unchecked. English or Hindi chooses the issued phrase language; it is not measured acoustic language coverage.

Current platform readiness remains false and waiting on us. The UI presents no checkbox demand or issue action while unavailable. Synthetic ready fixtures exist only to exercise the future producer. Source/model/identity authority remains server-owned. This change does not enable voice, publication, training or inference.

Before an issue, the component re-reads current readiness and compares every displayed binding. A replacement source, permission receipt or server snapshot clears all choices. Scope changes abort pending work. The actual Studio callbacks reject late replica/account/token results and unauthorized errors; each request retains the existing twenty-second deadline even when supplied a scope AbortSignal. Unconfirmed issuance requires an explicit authenticated read before another explicit attempt, with no application POST retry. A saved pending challenge is reused through existing status rendering.

## Evidence on 2026-09-08

- Actual mounted Journey/Capture/client plus the two exact TypeScript-AST-extracted StudioApp callbacks: 24 checks, 12 each at 390 and 1440 pixels. One Chrome, synthetic localhost JSON API, no real auth/model/SQL. Receipt: scratchpad/selected-reference-comparison/1788819592661/result.json. Includes all-unchecked choices, exact three comparison statements plus existing five, Hindi selection, current-source payload, exact historical producer negative, source and receipt replacements, late A readiness, account/token/unmount abort with late401, malformed descriptor and interrupted response body with explicit readback.
- Client11 uses the actual transpiled client with a recording transport and controlled AbortSignal timeout: deadline and caller cancellation for read/issue, removed-deadline negatives, newline rejection for all four bindings, and allowlisted payload. It proves client behavior, not a network timeout duration measurement.
- Existing clone-verification-journey/run.mjs:31 checks passed. App TypeScript uses --noEmit --incremental false, avoiding writes into reused dependency storage. Final types/copy outcome is recorded in the handoff manifest.
- Screenshots inspected: blocked390 shows the honest platform block without checks; permission390 shows the unchecked existing and comparison permission rows. The latter is the scrolled permission region, not a complete visual acceptance of the entire onboarding shell. Existing long legal copy is preserved.

## Retained failures and limits

Two fixture assembly failures ran zero browser checks: missing JSX brace and explicit virtual-import resolution without a .ts extension. Both were fixed only in the fixture. A subsequent socket-close case before response headers produced two native Chromium POSTs despite one application fetch call. Artifact1788819455669 preserves the actual two-request trace and returned phrase. This is not proof of application retry or backend duplicate issuance. Backend author confirms identical modern snapshot consumption is at-most-once, with stale replay refusal rather than same-response idempotency; real SQL acceptance belongs to that separate slice. The passing uncertainty control sends headers plus a partial body before disconnecting so it proves actual body uncertainty, not pre-header network retry behavior.

The first corrected batch passed20 then the fixture release helper waited for an abort already settled in the browser while the server socket had not marked destroyed. The final helper waits for the actual pending-fetch count, preserving the request barrier and ten-second action timeout. Failed artifacts1788819250517,1788819271208,1788819389819,1788819455669 and1788819530210 remain ignored and unaltered.

No durable consent is inferred from component state. No keys, hashes of private source content, user tokens or source IDs are displayed. The backend descriptor API is required for modern ready-positive behavior; an old unavailable envelope remains accepted. The fixed historical producer snapshots use pinned canonical-LF hashes and require no git history at test runtime.

Root should register selected-reference-comparison/run.mjs as a browser suite and selected-reference-comparison/client.mjs as an offline suite. Integration/full release, actual issued-authority SQL and calibrated same-capture acceptance remain separate required evidence.

Root visual review: these screenshots establish functional caller evidence, not final UX acceptance. A separate polish slice should simplify private-verification copy and use compact accessible checkbox grouping while preserving every explicit permission and blocked state. Keep actual recording name/date when supplied; remove server-verifier implementation detail from the product explanation. No late redesign is included in this freeze.
