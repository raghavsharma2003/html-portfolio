# Hindi observation53: source review candidate

Based on47e06c8341; no product or dist changes. Probe52 directly measured677ms inside the synchronous Hindi visibility function, matching native129.629ms style plus544.104ms layout. This change removes synchronous style/geometry reads from measurement, not native rendering from the product. No performance gain is claimed before the reviewed comparison.

The installer keeps bounded semantic DOM discovery and replaces synchronous CSS visibility reads with IntersectionObserver(trackVisibility:true,delay:100). Current candidates include actual associated enabled/nonreadonly correctly typed input, heading, label and actual parents of Hindi-bearing text nodes. Hidden/aria-hidden/inert nodes are excluded semantically; CSS opacity/visibility/offscreen/occlusion evidence comes only from renderer-delivered entries with isVisible plus positive intersection. Browser tracking unavailable means unavailable, never success. Bounds:64 text nodes per heading/label,132 unique candidates,256 generations. Overflow never passes.

Mutations invalidate prior entries and rebuild an observer per generation; callbacks from old observers cannot pass. The synchronous public readiness query drains pending mutation records before using cached entries, then rechecks semantic node identity/state. This catches direct disable/readonly/replace/hide changes before the MutationObserver callback runs. No style/geometry API, polling loop or extra layout request. Observer cleanup runs after gate first readiness and on reinstall/context disposal.

Gate subscribes to renderer readiness instead of calling visibility reads from its own mutation callback. The historic firstHindiPaintMs field remains callback receipt performance.now minus first-paint; entryTime and callbackTime are separately recorded in hindiVisibilityObservation. It is not Node time, selected glyph evidence or pixel-perfect paint. Never subtract100ms or backdate to entryTime. The renderer's explicit100ms tracking floor stays inside the unchanged800ms budget. New semantics are stricter for offscreen/occluded input, intentionally documented; actual390x844 coverage must pass a mounted check.

Source reference: https://w3c.github.io/IntersectionObserver/ Editor’s Draft, read2026-09-08. Renderer delivery avoids JavaScript synchronous visibility calculation; observer work itself is not claimed free.

## Checks and proposed experiment

Initial27 light controls passed on2026-09-08, no browser. Root review then required explicit constructor/observe failure handling; both now stop observers and record unavailable rather than throw an uncaught page error. All29 controls passed after that narrow addition: actual renderer positives, missing delivery, label/locale/root/input semantics, hidden/disabled/readonly/wrongtype, negative visibility/intersection, unsupported API,100ms option, old-generation and direct cached-positive invalidation, stop/cap behavior, and absence of forcing-read APIs. This fake renderer validates state/contract, not Chromium rendering.

Mounted script prepared17 actual DOM cases plus4 post-positive opacity/disabled/readonly/replacement transitions. It preserves original11 cases and adds offscreen/inert/opacity/readonly/association coverage. Main-world forcing-read APIs are replaced by throws; actual probe must pass without invoking them. Browser support is explicitly asserted; negative cases wait300ms only in the functional fixture, never in the gate. Positive delivery uses a bounded5s test timeout. Browser checks remain unrun while release56 owns the lane.

After root source review/lane grant: copy exact built47 dist/config/dependencies into private53, verify production source and assets identical, run mounted controls, then one fixed6-navigation old/new probe comparison for studio-hi (3 each, orderAB/BA/AB), cold contexts, originalload/networkidle/dwell/CPU/network/800ms budget. No product build is necessary. The odd number of pairs gives one revision two first positions; preserve the fixed order and all runs, no rerun. Plain gate budget functions evaluate both groups without changing thresholds. Neither a changed method nor lowerTBT alone establishes productperformance readiness.

Reversal: reject any false positive for missing/hidden/stale interface, unsupported success, new synchronous style read or unexplained product/asset difference. If actual readiness remains late, preserve the failed budget instead of changing100/800ms. Original45/47/49/52 failures remain historical receipts.
## Mounted receipt and explicit launch selection

2026-09-08T11:58:18.412Z: the one authorized full Chromium run passed all17 real DOM cases and4 stale-generation transitions. Chromium151.0.7922.34 executable SHA256409805a16d6416087e6b2f778df1cf8f7bbb267d6b99f6b5bb0a618eace234f2 is recorded before launch; forcing APIs stayed unused. Raw log scratchpad/observer53-mounted-full.log. The earlier default headless-shell launch died with0xC0000005 before any case; scratchpad/observer53-mounted.log is retained. A prior redirection failed because the private scratchpad directory was absent; it did not start a test. These are separate receipts, not a passing first attempt.

The pair now explicitly launches the same full Chromium executable it fingerprints (environment override or chromium.executablePath), with30s launch timeout. This closes a launcher fingerprint ambiguity in the draft runner: hashing full Chromium while allowing Playwright to select its default shell would not pin the launched binary. Both revisions will use the same exact executable and product/dist. This is a six-navigation method comparison, not acceptance or a historical full-gate reproduction. Await root review before launch.
## Fixed comparison result

Source freeze68cc960246a49508889100ae01f30898fe579051. The single authorized6-navigation comparison ran2026-09-08T12:03:31.221Z to12:04:13.770Z, exec55281, owned child27804. Terminal1, no deadline expiration or forced termination. Raw receipts: scratchpad/expert-tools/observer53-pair-20260908-once in the parent workspace. Product source/dist/package/lock/config and both helper/collector fingerprints stayed unchanged before/after; both arms used pinned full Chromium. No production code changed.

|Revision|Median LCP ms|Median TBT ms|Median Hindi receipt ms|Median CLS|
|---|---:|---:|---:|---:|
|Old synchronous probe|3360|1034|1861.5|0.089981|
|Renderer observation|2732|99|1710.9|0.086371|

Fixed chronological rows (LCP/TBT/Hindi milliseconds): A3484/1034/1861.5; B2732/101/1710.9; B2768/99/1817; A3004/1150/2016.4; A3360/964/1771.3; B2464/72/1494.9. Candidate TBT meets300ms; candidate LCP2732 exceeds2500 and Hindi1710.9 exceeds800. All three candidate readiness receipts were renderer-observed. No100ms subtraction. Both mediansJS147002/font84148/hiAuthCopy3183 bytes; one baseline Noto request remained pending at the exact receipt boundary, giving30262font bytes for that row. Candidate all3 fonts finished at84148, no cache, complete bounded collectors. This pending response is retained, not silently completed later.

Combined with52 direct caller attribution, this supports removing measurement-induced forced layout. It does not establish product native-layout optimization or release readiness. The instrument now observes a stricter rendered interface asynchronously; timing equivalence to old synchronous semantics is not asserted. Small fixed n3, odd first-position distribution, and one pending font limit interpretation. Original full45/paired47/native49/probe52 failures remain intact. No second pair or full gate was run. Next work must address the remaining actual Hindi visible-interface and LCP gap without raising budgets or backdating readiness.
