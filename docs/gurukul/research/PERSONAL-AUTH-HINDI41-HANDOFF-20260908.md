# Personal sign-in Hindi41: focused checks complete, release acceptance pending

Based on frozen39 b534fa93 in separate codex/auth-hindi41 worktree. This closes a deterministic product and measurement gap found during diagnostic40: /studio?lang=hi entered personalMain and the English-only StudioApp AuthGate, while the whole-body Hindi marker could match the aria-hidden Vyakti logo. Focused source, semantic TypeScript and mounted-browser checks now passed as recorded below. No font/layout performance experiment, production build, live provider call or release acceptance claim is included.

## Actual product path

The personal AuthGate now uses the existing studio locale preference order (explicit en/hi query, remembered choice, English) and existing lazy hiAuthCopy loader. A distinct typed personalAuth section reflects this screen's email-link-first flow; creatorStudio's different OTP-first messages remain separate. The Hindi auth chunk includes it while the rest of the Hindi studio table stays lazy. English and Hindi cover intro, email, pending/sending, optional code, cross-tab link check, Google, errors, resume destination, source-use/identity/model notice, image alt/captions and accessibility labels. No authorization predicate changed.

Main has lang and an explicit data-studio-auth-locale marker. Email values keep lang=en; language options have their own tags. A compact native language choice persists the existing preference and updates only the lang query parameter, preserving workflow query/hash/history state. Blocked history/storage writes do not prevent an in-memory choice. Existing brand-tag styling is preserved by retaining the tag as header's final span. Review found the generic text-button class inherits width100%/margin15px and is unsuitable inside the header. A dedicated auth-language-select class now provides min-height44px, widthauto88–112px, margin0, explicit text/background/border/focus colors, and permits the adjacent tag to wrap instead of forcing overflow. This narrow CSS correction is source-approved for the new control; no product font or speculative layout-performance experiment was made. Actual mobile/desktop geometry and contrast remain untested.

Opening/loading/error states use short bootstrap translations so unavailable Hindi does not silently render English. Failed lazy imports clear their rejected cached promise, enabling explicit retry; the user can explicitly choose English if Hindi cannot load. Language changes preserve typed email/code and flow state. Resume/code focus effects include locale readiness so delayed copy loading does not drop intended focus.

Provider errors no longer display arbitrary backend messages. Network,429 and service failures are localized and do not claim the code was wrong. Code input is preserved on those failures; only400/401 verification rejection clears it and asks for correction. The existing account operations and callback flow are reused. Independent source review caught that restoreSession swallowed refresh failures before the UI could classify them. It now offers a narrow opt-in reportTransientFailure mode used by this link check: transient errors preserve retry material and rethrow, terminal auth errors clear/null, and no unrefreshed candidate is returned as authenticated. Existing default callers retain their behavior. Callback retry material is retained only on the transient path after its URL hash was consumed. Actual session source and caller tests cover these distinctions but remain unrun.

## Truthful Hindi measurement

The historical firstHindiPaintMs wire key and800ms budget remain. A Hindi-only probe now requires main[data-studio-auth-locale=hi][lang=hi], visible Hindi text nodes in #signin-title and the actual email/code label, and the label's associated correctly typed, enabled, non-readonly input inside that main. Independent review found that a heading/label-only check allowed an absent or hidden input and hidden Hindi descendants. The corrected probe checks the control's CSS visibility and at most64 text nodes per heading/label, excluding hidden/aria-hidden/inert ancestors. Logo text, loading copy, a translated headline above English fields, or CSS-hidden controls cannot pass. This is a rendered localized-interface marker, not selected-font/glyph or pixel-paint evidence.

The visibility query may flush style. It is bounded to the associated input and64 text nodes per heading/label, explicitly disclosed rather than described as cost-free. No added sleep, font wait, throttle change, run-count change or budget relaxation. The import wait measurement is unchanged. All actual release39 failures and partial diagnostic40 traces remain valid historical receipts; their old logo-sensitive Hindi marker must not be relabeled as full interface localization.

## Source review plan (historical, before the executed checks below)

- New personal-auth-locale offline controls: actual bundled lazy copy and locale chain; nested parity/placeholders; source callbacks for send/verify failure classification and code preservation; URL/hash/history/storage behavior.
- New performance-hindi-interface controls: true interface, English plus Hindi logo negative (old scan falsely passes), heading-only, label-only, hidden/inert/CSS-hidden, unavailable interface, optional code and actual caller wiring.
- Additional source-prepared real DOM cases: actual email/code positives, logo-only English, missing input, display/visibility-hidden input, disabled/wrong-type input and hidden/CSS-hidden/aria-hidden Hindi descendants. These are in performance-hindi-interface-browser.mjs and remain unrun pending browser lane approval.
- Existing studio-locale assertions updated to three lazy auth sections and named bilingual blank-eyebrow exceptions. Existing magic-link source checks now follow copy definitions plus UI callers. Prerequisite fixture copies the new imported helper. New suites registered without removing any existing registration.
- Required after root source review: targeted offline controls, TypeScript/copy/graph checks, then one bounded real mounted English/Hindi email+optional-code+cross-tab+failure+locale round at390/1440 with actual UI and visibility-probe negative controls. Root owns scheduling. No claims until performed.

## Review risks and reversal

Using the existing English copy infrastructure may change entry bundle weight; unmeasured until the authorized build/gate. Hindi wrapping and native select geometry are unverified on mobile. Do not declare performance fixed or UI verified. Reverse or narrow integration if measured layout, accessibility, auth behavior, consent semantics or honest error classes regress. Preserve localization and truthful measurement rather than restoring the logo shortcut for a green result.

Review files: src/studio/StudioApp.tsx, personalAuthLocale.tsx, personalAuthCopy.ts, ExpertEntryVisual.tsx; src/creatorStudio/copy.ts and hiAuthCopy.ts; scripts/performance-hindi-interface.mjs and check-performance.mjs; related evals. No frozen39 or40 changes.

## Executed focused checks, 2026-09-08 UTC

After independent source review and parent lane approval, sequential offline controls passed: personal-auth-locale 46, performance-hindi-interface 16, studio-auth-link-code 10, studio-locale 95, performance-measurements 31, performance-prerequisites 9. Semantic `tsc -b --force` passed in 26.231 s (09:50:33.103Z to 09:50:59.335Z, exec30344). The earlier root `tsc --noEmit` invocation only checked an empty solution and is not semantic acceptance. Copy law subsequently passed seven scopes and 21 negative controls.

One authorized mounted batch exec63993 passed. Actual AuthGate: n=8 views, en/hi x390/1440 x general/test, 09:51:05.080Z to 09:51:26.575Z, 21.493 s. Real-DOM Hindi probe: n=11 positive/negative cases, 09:51:26.579Z to 09:51:28.435Z, 1.856 s. Raw supervisor receipt: scratchpad/focused-1788861065078/result.json. UI geometry/screenshots: scratchpad/personal-auth-locale-browser-1788861065537/result.json and eight PNGs. Each language selector is 88 or 89 px wide and44 px high; measured foreground/background contrast18.203:1, no header overflow, keyboard focus outline checked. Paths and hashes of actual eager styles and excluded deferred CSS are retained in css-inputs.json.

The mounted fixture loads actual AuthGate, locale/session code and eager CSS, mocking only account responses and blocking external requests. It covers send503 recovery, optional-code429 preservation and400 clearing, cross-tab refresh503 preservation and fresh200 acceptance. It preloads the dictionary and waits for fonts for visual checks: it does not prove lazy-load failure UX, live email delivery, provider operation, full application navigation or performance. Probe negatives include logo-only English, missing/hidden/disabled/wrong-type controls and hidden Hindi descendants.

Visual inspection of Hindi390 general, English390 general, Hindi1440 general and Hindi390 test confirms clear general-theme localized content and a visible language focus target. The test theme retains dark hero text on a dark green background and partial intro/card overlap; this is not covered by selector contrast assertions and is not claimed visually complete. No speculative unrelated theme edit was made. The scoped Impeccable detector ran once over StudioApp, personalAuthLocale, ExpertEntryVisual and auth-entry.css, returning an empty findings array (scratchpad/hindi41-impeccable.json). Its empty result does not overrule the observed test-theme limitation.

## Failures and fixture repairs retained

The first offline attempt failed before tests because esbuild was not a dependency; frozen39's private dependencies also lacked it. Offline installation of pinned esbuild0.28.2 failed ENOTCACHED, then the registry install succeeded with ignore-scripts. The exact development dependency and lockfile are included because the new fixtures import its API directly. No implicit npx-cache dependency remains. Private dependency copy ran09:43:45.431Z to09:46:23.870Z; receipt is ../expert-tools/hindi41-private-dependencies.json. Configuration was a verified blank stub, never project keys.

An existing magic-link assertion expected an already-absent baseline phrase; it now follows the actual localized resume caller and dictionary. Initial Hindi locale check passed94 and failed1 because the new legal translation contained banned jargon; it was replaced with explicit permission wording and the suite then passed95. These failures remain in focused-1788860905594 and focused-1788860964160. Final offline receipts are focused-1788861001021 and focused-1788861033101. No browser retry or performance run occurred. Integration must still build and run unchanged release budgets; earlier release39 failures and diagnostic40 evidence remain intact.
