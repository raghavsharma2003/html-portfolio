# Closing uncertain private request IDs

Separate candidate based on frozen authority1f8421ee. The original13-group candidate remains untouched. Migration142 was absent from55 locally present refs and21 worktrees during the source inventory, without remote fetch or live catalog inspection. Root must verify/apply it only to development.

A lost POST can leave a client UUID with no admitted request. Previously result and withdrawal both returned404 forever. An owner-scoped UPSERT now records that UUID as withdrawn even when no question, receipt, source or saved snapshot exists. It creates no consent. The existing request primary key makes a later admission fail atomically, including rollback of any receipt created by that attempted admission. An already cancelled ID never supplies compiler material or dispatch authority. A new question needs a new explicit tap and UUID.

Migration142 makes the ten authority columns nullable, but a check permits either all genuine authority fields or a completely empty withdrawn tombstone. Other states still require full authority, an encrypted question and known billing state. Tombstones retain non-null owner/replica/request IDs and inherit the existing owner index, replica FK cascade, explicit full-erasure delete and owner-export path. No source or consent needs to be invented to make erasure work.

Withdrawal first atomically creates/scrubs the terminal request and then uses a fresh statement to revoke existing exact owner/replica/request private receipts. A sibling CTE cannot see a receipt committed after its statement snapshot, even though UPSERT sees that conflicting request. Failure in the second step returns a named503 with the request handle; safe repeated withdrawal finishes it. The durable terminal row already prevents generation. No replica epoch increment or unrelated request invalidation occurs.

GET minimal terminal result is {replica_id,request_id,state:'withdrawn',billing_state:'unknown',can_voice:false,created_at}, with consent/source/answer absent. POST successful withdrawal always returns {replica_id,request_id,state:'withdrawn',private_payload_erased:true,billing_state,can_voice:false,created_at}. Known requests preserve the actual bound ledger state. An absent request uses unknown because source cascade can delete a previously charged request while spend remains. No refund or completed reconciliation is implied.

Executed2026-09-07:10 offline cancellation groups and16 incumbent store groups, Node syntax and diff checks. Offline tests cover scope, no-grant late admission, partial receipt failure/retry, terminal claim/complete refusal, real envelope-free export, known billing preservation and strict UUID input. No SQL or concurrency pass is claimed.

Root-only evals/private-text-cancellation-live.mjs exposes runPrivateTextCancellationSqlChecks({db,connect,onFixtureManifest,optIn:true}); connect returns {db,close}. It contains10 intended groups, including the exact historical1f8421ee unknown-withdrawal negative, nullable-authority constraints, owner/replica conflict, two actual transaction witnesses, known spend/source cascade, and direct replica cascade of source-free tombstones. Both sessions BEGIN before PID capture, use bounded local lock/statement timeouts, and require pg_blocking_pids(waiter) to name the exact writer before release. A savepoint models failed HTTP-statement rollback for expected23505 while retaining the pinned witness transaction. Every fixture/admission ID is manifested before writes, and cleanup directly recounts every scoped table, including private rows and synthetic spend. The harness has not run yet. No model, storage, identity, voice, publication or real owner grant is exercised.

Root's first migration142 development run applied the migration and passed the historical negative group, then stopped at the creation-time idempotency assertion. The WebSocket driver returns separate Date objects, so strict identity rejected equal instants. Cleanup reported0 fixture rows and no errors, with2 manifests. The harness now compares finite epoch milliseconds for both Date and HTTP string representations. Four offline controls passed: distinct Date objects at the same instant, Date versus ISO, a one-millisecond difference, and invalid input refusal. Product and migration bytes are unchanged. The remaining real SQL groups are pending root rerun.
