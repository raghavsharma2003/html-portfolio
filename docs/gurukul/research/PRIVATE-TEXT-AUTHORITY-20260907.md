# Private text rehearsal authority candidate

Base: checkpoint20 da3ac2aeac29571ae45a4507d947b1cf603cf9c1. Migration141 is reserved, not applied by this agent. This isolate contains only authority/store, encrypted payload, mutation fences, erasure/export and evaluations. Root owns authenticated route, Azure orchestration/compiler and integration; a separate agent owns mounted UI.

An explicitly replica/owner-bound saved draft and one mine-authored text/markdown/PDF/DOCX Context Locker item must have a ready matching source, complete canonical text_span evidence, and live account capture/storage receipts. Three explicit question-specific private-text statements create one durable receipt and encrypted request. This grants no biometric, training, ordinary runtime inference, voice or publication authority. Required actual draft fields are name, identityWho and physics/chemistry/maths subjectDomain. No canonical producer means unavailable; raw text alone cannot substitute.

The request ID binds question, statements and selected snapshot. Admission confirms its durable row before returning compiler material. Replays never return dispatch material. Dispatch claims once and binds the exact existing provider budget reservation and prompt hash. Completion rechecks current receipt, source, draft, evidence and replica epoch; it requires the bound ledger to be settled or reconcile_required. Only a gated answer can be stored. Readback repeats current authority before decrypting. Unknown backend errors fail visibly with content-free handles. AES-GCM envelopes bind owner, replica, request, content hash and question/raw/answer role with a dedicated wrapped key. PRIVATE_TEXT_REHEARSAL_KEK_ID/B64 must be provisioned separately; no fallback key exists.

The direct replica UPDATE epoch predicate protects against a writer committing after a stale read. Actual draft/Mirror, context attribution/evidence/removal, account grant/revoke, Never-rule, source-delete and source-erasure callers change that epoch in their mutation statement. Source operations acquire source then replica. Explicit sheet ownership is mandatory because legacy agent-only sheets have no single replica epoch. Dedicated withdrawal locks the replica without incrementing the epoch and revokes/scrubs only its own request. Capture/storage revocation and full AI revocation scrub all matching envelopes. Source/item/replica FKs cascade private requests; full erasure names the table explicitly. Owner export decrypts exact scoped rows and fails visibly if it cannot read/decrypt them.

Executed on 2026-09-07: 16 offline groups using the actual canonical producer, AES-GCM and store functions, including admission/replay, five envelope scope-tamper controls, foreign-owner refusal, current-epoch/receipt refusal, transient backend classification, owner export and withdrawn metadata recovery. Node syntax checks and git diff check passed. Attempting the incumbent canonical suite stopped on this isolate's absent ignored api/_config.js; no secret configuration was copied. No TypeScript, browser, build, real SQL, provider or actual owner-grant proof is claimed here.

The opt-in evals/private-text-rehearsal-live.mjs accepts injected development DB and onFixtureManifest only. It verifies the exact development database name before mutations, writes ID manifests before fixtures/admission, EXPLAINs actual changed SQL, and directly recounts private requests, evidence, context text/items, sheets, sources, receipts, audits, replica and synthetic spend/budget after cleanup. Its stale-epoch negative is a deterministic interposed actual draft save, not overlapping transaction proof. Ten additional incumbent mutation statements receive parser-only controls. Root must run this harness and retain its receipt before SQL acceptance. Source erasure must be merged with checkpoint21's stronger primary-selection CAS and revalidated on the resulting bytes.

No production migration, paid call, identity/voice acceptance, automatic correction learning or private memory extraction is part of this candidate. Older pending requests have no guessed completion or automatic retry; the owner can withdraw and explicitly ask a fresh question.

## Development first-write finding and repair

Root applied migration141 only to the exact development database. The first wrapper rejected valid content-addressed v5 fixture evidence IDs before its accepted run; root corrected that wrapper. The next actual SQL run stopped at canonical evidence persistence, with zero remaining fixture rows and no cleanup errors. The incumbent coverage CTE read only the base table, which cannot see newly inserted rows from a sibling data-modifying CTE in the same PostgreSQL statement snapshot. The repair counts exact ID/hash matches from INSERT RETURNING alongside exact scoped existing records. Revised SQL harness has13 groups and explicitly checks first-write, idempotent replay, wrong existing hash refusal and an old-coverage negative that inserts but falsely reports failure. Sixteen offline groups reran green; the revised real SQL run remains pending. No concurrency claim follows from this repair.

The next root run passed the canonical phase, then an uncoded parser fixture error occurred because a synthetic lease token was31 characters and the real erasure helpers require32. Both parser tokens were corrected, and direct offline actual-helper calls confirmed that they reach the injected SQL seam. Cleanup remained0/errors[]. No real13-group pass is claimed yet.
