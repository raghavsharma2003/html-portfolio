# Hindi/Hinglish retrieval: checkpoint24 caller audit

Read-only2026-09-07 audit of checkpoint24 **3db85f82f9491322a8db2a62556cf39be8234937**, expert-integration. No tests, browser, builds, database, embeddings, model or network calls. Current STATE START and relevant decisions/rejections/measurements were read first. Historical measurements below were read from retained artifacts, not rerun.

## Recommendation

The smallest justified retrieval experiment is an **explicit, unpromoted Roman-Hindi query-alias arm in the existing approvedMirrorRecall lexical diagnostic**. Keep its exact authorization SELECT, raw evidence, eight-fact limit and600-character per-fact ceiling. Expand only a small reviewed set of unambiguous content-term aliases into the existing bound query-term parameter; do not transliterate/rewrite evidence, add vectors, call a model or enable production ranking yet.

This addresses a real retained failure, but **only in the private Mirror approved-memory lane**. It must not be sold as improved ordinary Feed-to-private-test retrieval: that lane explicitly selects one complete canonical source and has no top-k retrieval step. Public Rooms similarly provide the complete five-entry published Q&A set. The modern Meet conversation does not call approvedMirrorRecall at all. If the immediate priority is Feed-to-Meet answer usefulness, keep this as a small retrieval candidate rather than replacing the already-running source-representation/answer-adherence work with a general retrieval project.

## Exact caller map

| Surface and actual caller | Retrieved material and scope | Consequence |
| --- | --- | --- |
| `api/mirror-call.js:909` calls `cloneTurnFor`; helper at413, `approvedMirrorRecall(db,ownerUserId,replicaId)` at423; assembly at435 supplies `window.transcript` separately | `api/_experience-compiler/mirror-recall.js:29`: private facts from explicitly accepted claims, cited matching episodes, exact owner/replica/agent/person, current training consent, latest accepted decision, disclosure predicate, invalidation/retraction exclusions; order need_p then recency, limit8 | The available current question is not passed into recall. Query-aware lexical rank is possible only through explicit fourth argument strategy `lexical-shadow`. Adding query alone does nothing. The actual production caller still uses no strategy. |
| `api/_mirrorcall-reply.js:286` assembleMirrorReply, memoryFacts map around314, compiler memories around361 | Only selected fact bodies are injected into existing shared compile/gatedReply/think path; up to8x600 characters | Evidence selection failure precedes model answering. This is private owner memory, not permission to publish uploaded source facts to followers. |
| `src/studio/ExpertConversation.tsx:166` -> `src/studio/dialogueApi.ts:38` -> `api/replica-dialogue.js` -> `api/_replica-dialogue.js:186` generateOwnedDialogue | Active runtime core + `loadPrivateRelationshipSnapshot` + session history; snapshot at`api/_replica-runtime.js:685` reads relational state/patterns/rituals/currency/phrases/kin with exact agent/person and current runtime authority | No approvedMirrorRecall, Context Locker text or public-Q&A retrieval caller here. Changing Mirror ranking would not change this modern Meet path. Generator registry is directly Azure Foundry; readiness/activation remains unchanged. |
| `src/studio/PrivateTextRehearsal.tsx:83` readiness and explicit Ask -> `api/replica-text-rehearsal.js` -> `api/_private-text-rehearsal.js:38,50` | `api/_private-text-rehearsal-store.js:33` selection SQL and `selection` around57: exact owner/replica/sheet/item, owner-attributed extracted/mined supported text, source ready/hash match, current capture/storage receipts, contiguous canonical evidence covering complete body, max8000 UTF16; compiler gets that selected body | No lexical/top-k source search. Roman question against Devanagari chosen evidence is an answer-comprehension test, not a retrieval-ranking test. Retain exact selection and readiness commitments. No alias treatment needed to deliver already-selected evidence. |
| ordinary `api/_room-surface.js:1782` roomSay, recall around1969, published knowledge at1994 | Follower memory via`api/_room.js:458` dmRecall, current consent/disclosure exact recipient+agent, need/recency limit8. Separately`api/_room-knowledge.js:7` fetches all active published showcase rows ordered position/id limit5 for resolved room/replica/owner/agent; current snapshot rechecked before dispatch/delivery | Private memory ranking and public knowledge are separate. No cross-script public corpus-selection loss within current five-row bound. Do not substitute owner Mirror memory or generic vectors. `roomCitations` around3951 returns current published catalog, not proof of specific source use. |
| generic `api/memory.js:710` opRecall; tokenizer352, semantic leg around864 | Older generic conversational recall has keyword and optional embedding legs, with fact/person/agent filtering; writes around2258 and2452 create embeddings | It is a different corpus/authority contract. Accepted Mirror materialization (`_experience-compiler/relational-materializer.js:137`) has no embedding-writer caller. Cannot borrow this wider reader as a shortcut. |

## Existing measured evidence, exact limits

`evals/mirror-memory-frontier/baseline.json`:40 synthetic schema-typed read-only PostgreSQL CTE snapshots. Relevant older fact omitted when8 or16 newer distractors fill the eight slots. No observed boundary failure, no actual persisted lifecycle or authenticated API proof.

`comparison.json`, measured2026-09-06T18:44:03.587Z (September7 India):52scenarios x2arms=104actual SQL SELECT executions. Baseline misses required id1 in six cases: crowding8, crowding16, Hindi, Roman Hinglish, mixed-script Hinglish and cross-script. Lexical-shadow recovers the first five and still misses cross-script. Both arms return9,8,7,6,5,4,3,2 instead of required1 there. Boundary failures0 in the retained scenarios. Current checkpoint24 recall file SHA256 **11e72f676f0d349cc47330ba6492ed0004468cd9d077639187198ada4c035350** exactly matches the recorded source hash; no result is attributed to an unseen later recall revision.

The exact cross-script case in `scenarios.mjs:70-81` has fact name empty and body **अंशांकन की अंतिम तिथि बारह जनवरी है।** Query: **anshankan ki antim tithi kya hai?** Eight newer unrelated inventory facts, equal need_p. Both normalizers preserve script; PostgreSQL `simple` lexical matching has no shared terms. Other language cases deliberately have lexical overlap and do not establish semantic or transliteration quality.

Empty/no-match/stopword-only queries keep baseline order exactly. They can return irrelevant facts, so fallback equivalence is NOT abstention. `query-contract.mjs` is offline bounds/parameterization/opt-in coverage; README says default `run.mjs` is manifest-only NOT_RUN, not a retrieval pass. Actual SQL requires its explicit --live --compare and exact isolated-development database guard.

The older `evals/recall/run.mjs`19-query Hinglish battery and `api/memory.js:352` short-word/bigram repair concern token loss in a different recall lane. They do not bridge Roman text to Devanagari and are not evidence to copy that tokenizer into Mirror. Generic memory semantic retrieval is not an approved-Mirror embedding pipeline.

## Bounded proposed implementation, not performed

1. Add a retrieval-only, immutable reviewed content-alias map adjacent to mirrorRecallQueryTerms, behind a new explicit diagnostic strategy. Initial entries may cover unambiguous source-search terms (for example anshankan -> अंशांकन, antim -> अंतिम, tithi -> तिथि), but accepting only these three would overfit the known fixture. Define the finite candidate table before generating fresh held-out question wording. Require at least two independently supported content aliases for expansion if ambiguity cannot be resolved; otherwise keep original terms only. This deliberately trades coverage for avoiding false expansion.
2. Keep original normalized query terms and append bounded aliases within the existing total24-term cap, preserving NFKC/tokenization/input500-code-unit bounds and parameterized `unnest($4::text[])`. No changes to body/name, source record, citation, SQL scope, prompt wording, model, ranking limit or fallback. A term budget overflow must preserve originals first; expansion must not silently evict a technical identifier or number.
3. Preserve the existing baseline and lexical-shadow arm. Compare a separate alias arm over exactly the same schema-typed CTE SQL scaffold. Keep production Mirror caller unchanged until acceptance below. If promoted later, connect exact `window.transcript` in cloneTurnFor423; a tested helper without this caller is still dead. The caller change needs its own actual-handler test, including no query from a different window/session and unchanged scope/admission.
4. Files: `api/_experience-compiler/mirror-recall.js`; one small query-alias module if that keeps table/version separate; `evals/mirror-memory-frontier/scenarios.mjs`, `run.mjs`, `query-contract.mjs` or a dedicated aliases contract file. `api/mirror-call.js` is a later, separately accepted caller switch. No UI, private rehearsal, Room Q&A, embedding store or disclosure changes in this bounded candidate.

## Required negative controls and acceptance

- Retain the exact cross-script omission as an old-arm negative; candidate must retrieve required id1 within the same8slots. Include at least8 fresh predefined phrase/source pairs outside that exact calibration sentence, covering physics/chemistry/maths terms, swapped question word order, common Roman spelling variants and mixed English technical tokens. Publish required/forbidden IDs before running candidate. Report counts per scenario, not an overall multilingual quality percentage.
- Add lexical distractors that contain generic alias words but not the requested entity or relation; include two similar facts with different entity/date and a newer approved correction. Retrieve the designated source fact, not merely any item containing January/calibration. Preserve numbers, units and chronology in returned bytes.
- Exclude English confusables and ambiguous standalone Roman terms: the/he/is/am/in, main, kal, par, bal. English-only queries and identifiers must be unchanged. An unsupported spelling must not become a confident guessed alias. This follows the repository's measured `the` -> `थे` rejection; pronunciation-table reuse is not a retrieval contract.
- Repeat every existing owner/replica/person/agent, expired/revoked consent, latest rejection/supersession, missing citation/episode/participant, retracted/invalid fact control with an alias that strongly matches forbidden evidence. All forbiddenReturnedIds remain empty. Ranking never runs over a broader corpus before filtering.
- Preserve no-match/empty/stopword baseline compatibility and exact legacy SQL/parameter behavior with no opt-in. Keep results explicitly unsupported/no-match in the diagnostic; do not call unrelated fallback useful recall or change product abstention policy in this slice.
- Offline controls prove finite aliases, bounds, deterministic order, no provider/network, query-only nonactivation, SQL parameterization and current caller's unchanged default. Before any SQL-quality claim, root runs actual --live comparison against guarded development using existing read-only typed CTE harness; EXPLAIN alone only parses. Zero boundary failures and no regression among existing required-evidence cases are mandatory, plus all declared positive finite-domain holdouts and negative English/ambiguous cases meeting their IDs. Any failing case is retained; no tuning on an undisclosed holdout.
- Production promotion additionally needs actual accepted-claim -> current question -> supplied evidence flow and accept/supersede/retract/erase lifecycle controls through native owner APIs. Current CTE snapshots do not prove that. Model answer correctness, Hindi/Roman register adherence, latency and human usefulness remain separately unproven; no paid call is needed for this first retrieval candidate.

## Rejected routes retained

`context/rejected.md:16272`: keyword ranking is not semantic multilingual memory; no-match fallback is not abstention. `stored-vectors-are-not-connected-approved-mirror-memory` immediately below: generic vector corpus cannot substitute for exact approved claim/citation authority. `context/decisions.md:810`: embedding-first remains rejected absent relevant local evidence, and existing per-dyad corpus/latency measurements are not a reason to buy another inference leg. `roman-hindi-transforms-exclude-high-frequency-english-confusables` at6844: never reuse the incorrect unconditional the/थे mapping. No general reverse transliteration model, embedding migration, corpus rewrite, language-model expansion or public/private source mixing is proposed.

This report proposes one finite diagnostic improvement. It does not claim retrieval or answer quality improved merely by reading these sources.
