# Current correction editing and revision safety

Isolate codex/expert-feedback-reopen, parent cff35f0484a1b3c13ce0c96d933083dff870ab1e. Separate checkpoint21 candidate; do not edit the frozen history isolate.

The actual ExpertConversation caller conditionally mounts TurnFeedback. Previously reopening initialized empty ratings, reasons and wording. The next rating-only POST appended a higher revision without correction_hash, and the dataset reader uses the highest revision per turn. Encrypted historical evidence survived, but the effective correction disappeared.

GET /api/replica-feedback accepts exact replica_id and turn_id, derives owner from bearer authentication, and returns current client feedback plus decrypted statement. It uses the writer's existing complete-turn/active self-replica/current capability-version predicates. No arbitrary feedback-ID or learning-example route is exposed. Missing eligible feedback is explicit null; an unavailable turn or decryption failure is an error. no-store applies to all responses.

POST expected_revision is the reviewed revision, zero for first feedback. Omission is compatible only for a first write. Absence of correction retains current wording; a nonempty correction replaces it; clear_correction:true explicitly clears the new revision. Empty correction is rejected as ambiguous. Retained text is decrypted and re-encrypted with fresh envelope material bound to the new feedback ID. Historical envelopes and existing erasure cascades remain unchanged. The SQL mutation checks the prior revision, and unique(turn_id,revision) plus ON CONFLICT DO NOTHING prevents a second winner from the same snapshot. No migration is needed.

Modern and creator editors share guarded data handling, retain their separate presentation and localization, and read before exposing editing/saving controls. No private text is saved in browser storage. Synchronous scope checks hide old private drafts on token/replica/turn changes; generation guards reject late reads and save receipts. A failed or undecodable POST cannot auto-retry. Check saved correction explicitly fetches the current scope before another user action; remount likewise reads current server evidence. There is no stable POST operation ID in this existing API. Revision CAS protects late requests even if navigation occurs before the response, but a GET is a point-in-time snapshot, not a guarantee that no other writer remains in flight.

Existing replacement validation still requires a relevant mismatch. Retaining a correction while the owner changes all ratings to exact does not erase the statement; current dataset classification may instead mark the turn a positive evaluation. Explicit clear removes it only from the current revision, not the historical erasure scope. This change secures the correction loop; it does not apply corrections to prompts, activate a learned version, or demonstrate better next replies.

## Focused evidence

On2026-09-07:9helper/actual-handler injected-row groups;28incumbent feedback;33mounted groups(modern390/1440,creator390);12incumbent datasetUI;93locale;TypeScript/copy/diff passed. Exact oldcff35f0component/client negative passed. UI receipts are scratchpad/feedback-reopen-ui/1788784338392/result.json and1788784425729-old/result.json. The390px fixture fits; isolated component styling is not a full authenticated Studio design acceptance.

The first new UI run timed out because the fixture expected an owner change to close the editor. It stays open while protected data rehydrates; corrected that expectation and33groups passed. The first old negative build mixed a new creator hook with an old client missing a read export; isolating the old modern caller/client fixed the fixture and the loss reproduced.

Root SQL harness evals/feedback-reopen-live.mjs exports runFeedbackReopenSqlChecks({db,openSession,onFixtureManifest}). It checks the exact isolated development database before writes, manifests synthetic fixture IDs before insertion, EXPLAINs actual SQL, includes old-source and CAS-removal controls, uses independent connections with a bounded synchronization barrier for competing same-revision writes, checks turn erasure, and explicitly recounts feedback/exemplar/turn and prerequisite fixture scopes after cleanup. Synthetic KEK and text are fixture data. Actual SQL result was pending at initial source freeze; the receipt below supersedes that pending state. No provider/identity/model calls occurred.

Final mounted run passed36groups at scratchpad/feedback-reopen-ui/1788784554935/result.json after moving blank-edit validation before POST. It keeps the draft and explicit clear control available, and the three added controls verify no POST. TypeScript passed after this UI-only refinement; the three SQL-frozen source hashes stayed unchanged.

## Root actual SQL receipt

At2026-09-07T12:43:22.599Z root executed all8feedback-reopen SQL groups on vyakti_expert_integration_20260906 with frozen ddc7946f source,12prewrite manifests,remainingFixtureRows0 andcleanupErrors[]. Actual old writer dropped current wording; the fixed writer retained it; SQL CAS-removal control overwrote newer evidence; scoped reads, explicit clear, stale revisions and turn erasure passed. Two independent connections share a pre-write barrier; there is no pg_blocking_pids witness or backend transaction-overlap proof. ROOT receipt scratchpad/expert-tools/feedback-reopen-sql-1788784971242.json records exact unchanged3hashes. No actual owner/model/training or next-reply improvement acceptance.
