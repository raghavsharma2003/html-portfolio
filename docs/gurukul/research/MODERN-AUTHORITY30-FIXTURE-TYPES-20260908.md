# Modern authority SQL fixture type repair

Parent's first acceptance execution on a8daeca failed before seeding, at absence SQLSTATE42883 (2026-09-07T22:47:05.488Z). The immutable result is `scratchpad/expert-tools/modern-authority30-sql-1788821220349-e4912018-94d8-40f1-b035-c0e0ba8c86c1-result.json` in the original checkout. No fixture rows or provider calls were created. The prior eight production EXPLAINs did not cover fixture SQL.

Actual read-only catalog receipt `authority30-column-types-1788821271651.json` contains 298 ID columns. The new retained test projection contains 67 columns for the 16 fixture tables. Migration044 and the actual catalog agree: artifact selection `decision_id` is generated-always BIGINT; evidence decision IDs and person IDs are UUID. The original artifact absence query compared BIGINT to UUID[], and seed attempted to insert a UUID into that generated identity. No production query is changed.

Artifact selection absence now checks the predeclared owner, replica and artifact scope. Seed omits the generated column and uses `RETURNING decision_id::text`; validates a positive int8 decimal string, records it in `generated_artifact_decision_ids`, then awaits durable manifest persistence before further writes. No identity override or sequence manipulation is used. If persistence fails after INSERT, cleanup still uses the already durable replica/owner/person scope and normal cascades. Evidence decision IDs remain UUIDs.

The audit also found that processing attempts have only `job_id`, not replica/owner columns. Capture preparation now inserts attempts through a SELECT with the exact parent job/replica/owner predicate. Absence and final counts use predeclared job UUIDs; the actual job FK cascades deletion. All other fixture owner/replica comparison columns are UUID in the retained catalog. The fixture parser covers additional non-ID shape mistakes without assuming this source inspection is PostgreSQL validation.

`prepareLiveFixtureSqlCases()` records 42 distinct exact queries by invoking the actual absence, seed, capture and cleanup functions through recording callbacks. These callbacks are not SQL proof. The next real runner EXPLAINs these 42 and the unchanged eight production shapes in a read-only transaction before absence checks or seed. It retains the original artifact mismatch expecting42883 and the original attempt count expecting42703, alongside the existing invalid-column42703 negative. EXPLAIN uses no ANALYZE and does not consume an identity value.

Offline evidence: 14 live-harness controls and seven schema/fixture controls pass. New controls cover generated identity above JavaScript's safe integer range, invalid identities, missing durable callback, interrupted post-INSERT manifest with exact cleanup, fixture parser refusal before seed and deterministic query hashes. Previous witness, cleanup, collision, wrong-database and source-pin controls remain. These tests do not establish actual SQL lifecycle/race acceptance, identity verification or voice quality. The next parent execution is pending.

Launcher follow-up: use a new filename/freeze. Retain the prior failed launcher and result. Validate `generated_artifact_decision_ids` as an append-only list of positive int8 decimal strings; `decision_ids` now contains only UUID evidence decisions. Pin the new fixture-SQL module and complete dependency closure. Migration144 already exists in the development database; do not apply it again. Root alone executes.

Decision: use database-generated identity semantics and actual fixture parser coverage, rather than changing schema or coercing mismatches into text. Reversal requires contradictory actual catalog/parser evidence or an equivalent narrowly scoped fixture construction preserving generated IDs and cleanup. Reject the earlier assumption that UUID-shaped in-memory artifact decision fixtures model SQL storage types.

## Follow-up: preserve timestamp transport precision

V3 root actual run reached59passing checks, including all8production+42fixture EXPLAINs and3negatives, then failed valid-load22007. All3x16cleanup counts were0, five sessions closed without errors,282queries and0provider calls. Immutable result SHA b71b32fed39ec5333264170cd3e246a6c6c7442b2b09b131d862c104658ad4d3.

The installed Neon Client OID1184 parser returns Date for timestamp text. Offline actual-parser reproduction of 2026-09-08 12:34:56.123456+00 yielded Date; canonicalJson({leaseExpiresAt:date}) yielded {"leaseExpiresAt":{}}. Date.toISOString yielded only .123Z, losing microseconds. The native production sweep imports q from api/_db.js; its HTTP JSON response preserves timestamp strings. Production assignment remains unchanged; other injected Date-based production consumers are not proven compatible.

The fixture UPDATE now RETURNINGs verification_lease_expires_at::text with the same alias. A strict string/time-shape guard preserves the entire PostgreSQL/ISO timestamp and refuses Date objects rather than inventing lost precision. No lease duration, expiry predicate or exact timestamp equality changes. The42fixture query inventory automatically pins this changed RETURNING shape. Offline14harness+9schema/fixture controls pass, including retained Date/ISO-loss negatives and actual attach handling. Actual new SQL acceptance remains pending; no service calls by agent.

## Follow-up: exact artifact-review guard closure

V4 parent execution passed63checks including valid-load and three batch-review controls, then stopped at review-writer-epochs ERR_ASSERTION. All48cleanup counts were0, seven sessions closed without errors,315queries and0providers. Retained result SHA23dced3c2f51b0d02a45639a78ec61352ff7a6fd776b9c403b0a60956300c611. Offline replay of the actual frozen artifact-review query against launcher guardV3 reproduces unrelated_table_refused before SQL submission: its existing stale-build/genome retirement CTEs touch two tables absent from the guard. This result does not establish a production eligibility or epoch defect.

New guardV5 permits model_build/voice_genome writes only when the full original SQL SHA is e3f5fc6e73e698493516f3b01263353a08d39e122189d1468495dbc8d99727e3 and the existing declared owner/replica parameter guard passes. Neither table is globally allowlisted. No model/genome fixtures are added. Harness absence and final counts now include both; VoiceGenome has replica_id only, model build has owner/replica UUID tuple. Both actual FKs cascade. Retained catalog projection now71IDcolumns for18tables. Fixture inventory45shapes, unchanged8production shapes,3parser negatives.

Offline13actual-production-query guard controls retain the old rejection, cover all8queries and refuse changed SQL, foreign scope, missing manifest and EXPLAIN ANALYZE.14harness+9schema controls remain green. Review-artifact stages now separately name prerequisites, selection, reference-epoch and private-epoch checks without messages/parameters; +1 reference and unchanged private assertions are unchanged. No live SQL by agent. Next root execution pending.
