# One actual uploaded-source answer

7 September 2026. Frozen checkpoint24, `3db85f82f9491322a8db2a62556cf39be8234937`. Root-native run: 17:22:29.413Z to 17:23:50.224Z. One synthetic account, one uploaded file and one actual Azure model dispatch. This is a bounded functional example, not a real expert likeness or general language benchmark.

The file describes synthetic worksheet LATTICE-N84: checking takes 18 minutes, then graphing takes 25 minutes, together 43 minutes. Group size is not specified. The independently authored question requests those times, the event order and student count in Roman Hinglish.

Delivered answer:

> LATTICE-N84 worksheet mein checking ka time 18 minutes tha aur graphing ka time 25 minutes tha.
> Dono milake total 43 minutes lage. Pehle checking hua tha, uske baad graphing shuru hui.
> Lekin group mein kitne students the, ye note mein specify nahi kiya gaya hai.

Root and independent review found correct quantities, units, total, chronology and explicit uncertainty about student count. The answer uses understandable Roman Hinglish. Delivery adds line breaks to the raw response without changing its facts. The answer does not establish personality fidelity, fine-tuning, voice quality or improvement from saved teaching guidance.

## Operational evidence

Actual Supabase authentication, Azure private file storage/readback, canonical extraction, private draft save and exact selected-source readiness preceded the actual private-question handler. It created its own question-specific permission and encrypted/gated answer. GET and same-ID POST replay returned the same answer with no second model call. Explicit withdrawal scrubbed the question, answer and raw envelopes, while preserving settled usage. Logical source removal made the source unavailable.

The answer cost 539 microUSD ($0.000539). The development ledger moved from 128,467 to 129,006 microUSD, with zero reserved; prior receipts were preserved. This covers this text answer, not infrastructure cost or a per-customer forecast.

## Cleanup limitation retained

The original run ended `cleanup_pending`. Azure object removal returned 202, subsequent HEAD returned 404 and the exact prefix was checked. The storage writer is released. The new test guard nevertheless rejected the existing source-completion SQL before forwarding it because that SQL also updates consent rows. This is an observed harness restriction, not an established production SQL failure.

The original result, helper and manifest remain immutable. A separately frozen cleanup-only continuation must allow only that exact scoped completion, prove its receipt and clean the synthetic database/account records. No repeated question or model call is authorized by that continuation. Until its actual result is recorded, physical storage absence is not complete cleanup acceptance.

Original artifacts: ROOT `scratchpad/expert-tools/native-feed-answer-run-checkpoint24.json` and companion manifest. Frozen rubric: `NATIVE-FEED-ANSWER24-RUBRIC-20260907.md` in the same directory.

## Cleanup continuation completed

At 17:31:23.895Z, the separately frozen cleanup-only run completed: source erasure attempt 2, physical absence and audit receipt confirmed; all 98 database scopes empty; synthetic auth account absent. It made zero model calls and preserved the $0.129006 cumulative development ledger, with zero reserved. Artifact: ROOT `scratchpad/expert-tools/native-feed-answer-cleanup-checkpoint24-resume1.json`. The original failure remains retained; the continuation does not rerun or improve the answer.
