# Copy gate execution and baseline findings, 2026-09-07

Execution-only candidate from c56cadfe72a20ee02781485752d8d67fcfc6fb21. No product copy, vocabulary regex, allowlist, scan scope or release acceptance changes.

`scripts/check-copy.mjs:51` imported a URL pathname as a filesystem path. On Windows that produces a leading `/C:/` which filesystem operations resolve as `C:\C:\`; encoded spaces and percent characters are also not decoded correctly. Its main comparison at line542 constructed `file://` plus a platform path. Direct Windows invocation therefore exited0 without running the scanner or its negative controls. The repair uses `fileURLToPath` for the root and guarded `pathToFileURL(process.argv[1]).href` for the main-module comparison.

`node evals/copy-gate-portable/run.mjs` passed9 actual child-process/filesystem groups on Windows Node24.13.0. Retained original code is the negative control: the same bad fixture exits0 without output; forcing its main block then fails ENOENT before scanning. The candidate scans relative and absolute entry paths from an unrelated working directory, including real directory names with spaces, percent, hash and Hindi characters; an imported module stays inert, and actual dash/vocabulary violations fail until removed. Exact source reconstruction proves the only changes are URL imports and the two conversions. All21 original scanner negative controls run in each clean CLI fixture. Tests clean their exact newly created temporary directory after checking its resolved parent and prefix.

The actual repository command now exits1. It finds125 Rooms-vocabulary occurrences across27 files:124 in Studio,1 in creatorStudio. No other rule fires. Full output is retained at scratchpad/copy-gate/actual-full-scan.log. The earlier combined audit compared every occurrence's rule/text with untouched c56cadfe and found zero new violations. This is not a full copy pass. Fixing the gate is necessary even while disposition of those findings is unresolved.

## What the findings mean

The scanner's source-visible findings are largely real display text or accessibility labels, not identifiers. This audit does not claim every conditional panel was mounted or reachable in a live owner session. No fixture or generated code appears in the27 reported paths. Main concentrations are StudioApp23, CloneExperience10, CloneVerificationJourney8, QuickStartPath8, EnrollmentWorkspace7 and ModelConsentGate7. No Room or landing-page violation appears in this retained scan.

- Current owner experience: `CloneExperience.tsx:668` and `:669` name the clone drawer; `StudioApp.tsx:1034` starts a new clone. Those are intentional present product nouns and direct hits under the old rule. They are not lexical false positives.
- Technical labels: `RuntimeGate.tsx:101`/`:103`, `ProcessingReview.tsx:177`/`:178`, `PersonModelStudio.tsx:91` and voice experiment panels expose model/version/evidence terms. Some are useful in advanced evidence review; ordinary-owner clarity should be reviewed at the actual caller rather than exempting entire files.
- Consent and recovery: `ModelConsentGate.tsx:85` displays the actual `PAUSE MODEL` confirmation token, and `:106` explains separate training/inference durations. `EnrollmentWorkspace.tsx:700` and `:1084` explain withdrawal. Bulk word replacement could change the confirmation contract or obscure distinct permissions. Existing statement and receipt semantics must be preserved.
- Exact disclosure exception gap: `src/creatorStudio/DisclosurePreview.tsx:42` contains the spoken AI-clone disclosure. `scripts/roomsVocabAllowlist.mjs:25` already allows its phrase in `src/studio/DisclosurePreview.tsx`, but not the creatorStudio path. This is an exception-scope mismatch worth a narrow review, not evidence that the spoken disclosure should be rewritten. The source was reviewed; no new legal authority is inferred.
- Source extraction is approximate: the Mirror caption finding at `MirrorCallStudio.tsx:896` includes adjacent markup in its preview, but the caption itself contains the banned noun. That imperfect snippet does not invalidate the underlying hit.

## Product decision trace

`context/decisions.md:8332`, `ws-r10-rooms-vocabulary-gate`, dated2026-09-03, explicitly bans clone/replica/model/training and related terms throughout Studio as well as Room. The rule is not accidentally global: its source scope at `scripts/check-copy.mjs:68` does exactly that. Reversal requires a product vocabulary change and corresponding documented rule/allowlist update, not a quiet exception.

`horizontal-platform-reweight` at `context/decisions.md:4062` is the explicit owner directive for a horizontal clone platform, with edtech as first vertical. Its August26 date predates the September3 Rooms rule, so chronological supersession cannot be inferred from that entry alone. The currently active September7 integration STATE START HERE says expert-first AI with owner context, voice, reviewed corrections and private client continuity, retaining Rooms. Root additionally relayed the current owner request for expert AI cloning. That is a reason to review whether the old universal Rooms naming fits the modern owner product, not a source-supported decision to remove every technical-term constraint.

The separate binding baseline in `docs/gurukul/DESIGN-LAW.md:25` bans em/en dashes, filler, fake metrics and internal product leakage, and requires truthful plain copy. It does not itself supply the later Rooms noun list. A scoped product naming decision can distinguish modern owner preparation, advanced technical/permission evidence, and follower-facing Room copy while preserving those laws. No such rule change is included here.

Recommended next review: choose explicit vocabulary per actual owner/follower surface; preserve or narrowly document exact legal/token exceptions; then repair only proven unwanted copy at actual callers with scoped negatives. Do not waive the125 wholesale, convert the gate to warnings, or bulk rewrite consent words to manufacture a green release. Reversal of this execution patch is justified only by a reproduced entry/import/path regression under supported Node; policy disagreement belongs in the separately versioned rule decision.

No DB, provider, browser, build, identity grant, deployment or model call. Full release remains unaccepted by this candidate.
