# Fresh private comparison preparation31

This source slice implements the authenticated upload/finalize caller and a purpose-bound seven-stage worker path. It has not run against PostgreSQL or a real provider. Production readiness remains `comparison_gpu_accounting_unavailable`; no identity, liveness, clone likeness, public voice or training permission follows from preparation.

## Actual path and authority

`replica-source.js` accepts `create_upload` with purpose `comparison_reference`, stable matching `preparation_id`/`upload_intent_id`, ordinary audio/video metadata, at most 32 MiB, no third parties, and three separate true attestations: `recording_is_only_me`, `process_for_private_comparison`, `no_training_or_public_voice_permission`. It derives the owner from authentication, creates/replays the source, then writes the immutable purpose receipt before issuing an upload capability. Existing capture/storage receipts are required; self-test bootstrap is skipped. Upload and finalize keep their existing response shapes. The same stable ID recovers status through authenticated `POST /api/replica-comparison-preparation` with `op: status`; `withdraw` revokes authority and invokes existing fenced source deletion. An unknown withdrawal retires the ID and recovers any pending source by exact upload intent.

The response is `{preparation:{preparation_id,source_id,state,expires_at,completed_receipt_sha256,can_voice:false},readiness}`. Source ID is nullable for an unknown withdrawal tombstone. State is authorized, queued, running, prepared, revoked, expired, failed or reconciliation_required. Readiness is always `{available:false,waiting_on:'us',code:'comparison_gpu_accounting_unavailable',can_upload:true,can_prepare:false}`. `readiness` operation returns only that object. No response contains vectors, evidence payloads, consent receipt bodies or storage paths beyond the existing upload contract.

Finalization links an integrity job to the preparation. The existing scheduled consumer leases it; `runtime.js` loads current purpose authority, then `worker.js` executes integrity, malware_scan, media_probe, diarize, separate, enhance and voice_quality. Transcription and model build are excluded. Probe duration must be 1..60000 ms before model stages. Existing honest single-speaker separation passthrough may avoid a model call. At most one durable dispatch exists for each of four model stages. Signed ECAPA, xvector and VAD revisions and actual embedding dimensions must match the existing reference contract. Historical missing revisions remain unavailable.

Source, owner, replica, source hash/bytes/MIME, exact capture/storage receipt IDs, policy and the observed `private_text_epoch` bind the purpose receipt. Any private authority epoch change conservatively invalidates this preparation; this slice does not change that epoch or grant automatic renewal. Source and replica locks precede dispatch/completion checks. Successful processing completion writes artifacts, evidence, job receipt and preparation receipt in the existing atomic commit statement. Revocation and expiry block private reads, provider admission, writer renewal and completion. Real lock/snapshot behavior remains to be proved.

## Schema and erasure

Proposed migration146 is not applied. It adds `vy_replica_comparison_preparation`, `vy_replica_comparison_dispatch` and content-free `vy_comparison_preparation_id`, plus the nullable job linkage and comparison source purpose. Composite source/job/preparation/owner foreign keys cascade private rows. The ID ledger intentionally survives owner/source erasure, preventing re-use after cascade. Null receipt/hash checks are explicit; PostgreSQL CHECK's unknown result must not admit incomplete authority. Existing source erase/full-replica cascade and writer horizons govern physical objects. Capture/storage revocation and source deletion mark preparations revoked and scrub completed receipts. Creator export includes the two private tables and refuses their SQL failure; full erasure names both exact owner tables, including source-null tombstones. The content-free ID ledger is excluded. Relcheck discovers the cascade from the live catalog, but must be run after applying146 to the exact development database.

## Meter boundary

`comparison-dispatch.js` accepts only an injected server meter with kind `azure-container-infrastructure/v1` and reserve/begin/settle/markUncertain/releaseBeforeBegin methods. No production factory or environment switch supplies one. Missing meter refuses before a model adapter/private-input read, GPU readiness wake or POST. The real Azure evidence adapter calls the admission hook before its health wake, signs after the wake, and retains response accounting even for a charged failure. Durable step claim precedes begin. Lost begin/settlement responses become reconciliation_required and cannot retry or release as unspent.

This interface is not evidence of metering. Existing provider-budget accounting has no genuine GPU allocation meter. Parent ARM evidence reports scale-to-zero T4 configuration and one-minute replica/utilization monitoring, not allocation seconds. Parent's retail allocation-window estimate is not an Azure charge. A future resource-window adapter must reserve conservatively for startup and allocation tail, own an exclusive bounded resource window, and reconcile genuine usage/cost receipts. Never use HTTP duration, utilization integration or a reservation estimate as settled bill. No GPU can dispatch from this source slice as shipped.

## Completed consumer contract and remaining actual caller

`readOwnedCompletedComparisonPreparation(db,owner,rid,sourceId,voiceQualityJobId)` returns null or `{preparation_id,completed_receipt_sha256,receipt_sha256,expires_at,completed_receipt}` after current authority and exact job-result checks. The completed receipt is the canonical processing manifest: schema_version, step, artifact_ids, evidence_ids, next_steps, verified_input_sha256, purpose, preparation_id, preparation_receipt_sha256 and manifest_hash. `completed_receipt_sha256` equals the manifest hash over its basis excluding its own hash; it is not a hash of the JSON including that hash. This helper is the frozen seam for145, not yet an actual145 caller in this isolate.

Current145 requires an ordinary primary source and primary_selection_id. Fresh comparison-only material is deliberately excluded from that setter, ordinary list, review decisions, self-test, build intents and model builds. The coordinated follow-up must discover completed146 sources independently, bind their purpose receipt in145, use the selected145 reference ID as dedicated comparison selection authority, and branch modern descriptor/issuer/UI explicitly. Checkpoint owns that seam and fresh upload/status UI in the existing verification step. Reusing the ordinary primary mutation would allow comparison-only material to displace training material and is rejected. Do not call the fresh owner journey complete before this seam and native caller proof land.

## Verification and next acceptance

On 2026-09-08, `node evals/comparison-preparation/all.mjs` passed26 groups:15 authority/runtime/dispatch controls, one actual seven-stage chain with synthetic signed adapter transport/meter/window/SQL, five actual source handler/store callbacks, one ordinary primary predicate control with a removed-predicate negative, and four authenticated status/readiness controls. The chain made3 fixture POSTs, zero real calls. Incumbent processing suite, voice evidence25, processing worker45, processing sweep44, review38, self-test31 and storage writer22, model build17, primary voice17, source erasure59 and creator export57 passed. Two processing-worker fixture dispatchers were narrowed to distinguish the new commit CTE from a source SELECT; original lost-lease/output deletion assertions remain. Empty ignored config exports resolved an import-only environment failure. No browser, full release, SQL, provider or likeness test ran.

Root must first review/apply146, EXPLAIN actual new and modified query shapes, then run exact synthetic scoped lifecycle and witnessed two-session controls: owner/source/job FK mismatch; null receipts; stable replay; failed authorization preserves old state; unknown withdraw vs admission; revoke/delete vs private read/claim/commit; expiry; same-step duplicate dispatch; lost claim acknowledgement with no begin; lost begin and settlement no retry; four-dispatch bound; source/replica epoch change immediately before lock acquisition; failed ordinary primary selection leaves pointer and epoch unchanged; completed-receipt tamper; exact private-row cleanup with retained ID count. Record SQLSTATE failures as failures, never authority refusals. No global sweeps or provider calls are implied by this plan.

Reversal: reject this slice if those real CAS/cleanup proofs admit stale authority, any ordinary caller consumes comparison-only evidence, absent accounting wakes a GPU, or any uncertain dispatch is reissued. Preserve failed evidence and repair the concrete boundary before enabling any capability.
