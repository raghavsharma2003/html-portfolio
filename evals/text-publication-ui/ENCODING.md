# Classic-script fixture encoding regression

2026-09-08; tested renderer base3c8db8120bf66eb280540d3c742bbea6f299f89d. Production is unchanged by this fixture repair.

The unchanged incumbent publication suite initially timed out at run.mjs line69 waiting for Teaching profile, before its first group. Its saved classic IIFE bundle passed Node syntax checking. A passive browser replay of that exact bundle captured Invalid or unexpected token, empty #root and no HTTP API requests. This was not a failed owner readiness response.

A two-page diagnostic served identical JavaScript bytes and the same fixture responses. Without a document charset, actual document.characterSet was windows-1252 and the script failed before React. Adding only the UTF-8 meta declaration produced UTF-8, no page errors, the owner Teaching profile selector and one readiness request. Both production studio.html and index.html already declare UTF-8. The fixture had omitted that production prerequisite; adding the Unicode-bearing math dependency exposed its classic-script decoding defect. The diagnostic and original failure are retained under scratchpad/math-consumer-incumbents29; neither is rewritten as a pass.

The fixture now declares UTF-8 normally. A test-only legacy query omits it, allowing the same compiled script to reproduce the old decoding failure. The next page uses the normal declaration and proves owner readiness mounts. Both controls precede the unchanged28 incumbent owner/visitor, recovery, session and withdrawal groups. No assertions, deadlines, API response shapes or product authority gates were loosened.

Executed sequentially against the frozen renderer:

- dialogue-history-ui.mjs:24 passed, receipt scratchpad/dialogue-history-ui/1788817639467/result.json.
- feedback-dataset-ui.mjs:12 passed, receipt scratchpad/correction-ui/result.json.
- conversation-setup-ui.mjs:34 passed, receipt scratchpad/meet-setup-ui/1788817665870/result.json.
- text-publication-ui/run.mjs: initially exit1 before any group, log scratchpad/math-consumer-incumbents29/text-publication-ui.log and bundle scratchpad/text-publication-ui/1788817688395/bundle/fixture.js. Repaired full suite30 passed (28 existing plus2 encoding), receipt scratchpad/text-publication-ui/1788817906787/result.json.

Only the approved blank config stub SHA256728dc5821336bed9c5ad851b3e837a54d342674923de81454ce11b0a2d48a432 was verified and copied from frozen28. No credential config was copied. All browser responses were local synthetic fixtures; this is actual consumer integration coverage, not real auth, SQL, provider, publication or answer-quality evidence. No new44 renderer rerun, full release or production edit was required.

Decision: match the production UTF-8 document boundary in classic-script browser fixtures and retain a measured unchanged-bundle negative. Reversal: if the shipped document/bundle decoding contract changes, reevaluate the fixture with actual byte/charset evidence. Rejection: labelling the timeout an environment/resource issue or changing the owner selector would have hidden an actual decoding cause; Node parse success alone did not prove browser decoding. Root owns context and registry integration; the existing suite key and command are unchanged.
