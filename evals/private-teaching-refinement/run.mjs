// Actual result -> refinement -> next-question caller, synthetic localhost only.
import assert from 'node:assert/strict';
import {readFileSync,writeFileSync,mkdirSync} from 'node:fs';
import {join,resolve,extname} from 'node:path';
import {fileURLToPath} from 'node:url';
import {execFileSync} from 'node:child_process';
import {createServer} from 'node:http';
import {build} from 'vite';
import {chromium} from 'playwright';
import ts from 'typescript';
const root=fileURLToPath(new URL('../../',import.meta.url)),art=join(root,'scratchpad/private-teaching-refinement',String(Date.now()));mkdirSync(art,{recursive:true});
const RID='10000000-0000-4000-8000-000000000001',SHEET='20000000-0000-4000-8000-000000000001',ITEM='30000000-0000-4000-8000-000000000001',SOURCE='40000000-0000-4000-8000-000000000001',CONSENT='50000000-0000-4000-8000-000000000001',REQUEST='60000000-0000-4000-8000-000000000001';
const H='a'.repeat(64),NEW='b'.repeat(64),NEXT='c'.repeat(64);
const statements=['authorize_private_text_question','understand_ai_text_only','understand_private_retention_and_withdrawal'].map(id=>({id,text:({authorize_private_text_question:'I authorize this private question with the selected material.',understand_ai_text_only:'I understand this is AI text only.',understand_private_retention_and_withdrawal:'I understand retention and withdrawal for this question.'})[id]}));
const initial='diagram -> relation -> worked example';
let value=initial,currentHash=H,epoch='1',scenario='normal',resultMode='complete',pending=[],requests=[],browser,server;
const checks=[],errors=[];
const review=(rid=RID,request=REQUEST)=>({replica_id:rid,request_id:request,sheet_id:SHEET,sheet_hash:currentHash,sheet_version:'private-v1',private_text_epoch:epoch,field:'explanationOrder',value,updated_at:'2026-09-07T00:00:00Z',can_save:currentHash===H,...(currentHash!==H?{blocker:'private_refinement_review_changed'}:{})});
const result=(rid=RID,request=REQUEST)=>({replica_id:rid,request_id:request,...(resultMode==='complete'?{state:'complete',answer:'The period is 2 seconds: 24 seconds divided by 12 oscillations.'}:resultMode==='stale-complete'?{state:'blocked',failure_code:'rehearsal_inputs_changed',can_review_teaching:true}:resultMode==='unmarked-stale'?{state:'blocked',failure_code:'rehearsal_inputs_changed'}:resultMode==='source-blocked'?{state:'blocked',failure_code:'rehearsal_source_unavailable'}:{state:resultMode}),consent:{consent_id:CONSENT,receipt_hash:H,statement_set:'private-text-rehearsal/v1',expires_at:'2026-10-07T00:00:00Z'},source:{sheet_id:SHEET,sheet_hash:H,context_item_id:ITEM,source_id:SOURCE,source_hash:H,evidence_hash:H},billing_state:'settled',can_voice:false,created_at:'2026-09-07T00:00:00Z'});
const readiness=rid=>({replica_id:rid,state:'ready',can_ask:true,blockers:[],drafts:[{sheet_id:SHEET,name:'Anjali',updated_at:'2026-09-07T00:00:00Z',status:'draft'}],context_items:[{item_id:ITEM,source_name:'pendulum.txt',status:'extracted',eligible:true}],selected:{sheet_id:SHEET,sheet_hash:currentHash,context_item_id:ITEM,context_hash:H,source_id:SOURCE,source_hash:H,evidence_hash:H,authority_epoch:epoch,snapshot_hash:currentHash===H?H:NEXT,material:{draft:{name:'Anjali',identityWho:'Physics teacher',subjectDomain:'physics'},context:{source_name:'pendulum.txt',format:'text',body:'A pendulum completes 12 oscillations in 24 seconds.'}}},statement_set:'private-text-rehearsal/v1',statements,grant_scope:'private_text_rehearsal'});
const check=name=>{checks.push(name);console.log('PASS '+name);};
// Run the actual public validator and transport functions with their fetch leaf injected.
let response,wire;
let client=readFileSync(join(root,'src/studio/privateTeachingRefinementApi.ts'),'utf8').replace('import { replicaRequest } from "./replicaApi";','const replicaRequest = (...args) => globalThis.refinementHttp(...args);').replace('import { isPrivateTextId } from "./privateTextRehearsalApi";','const isPrivateTextId = v => typeof v === "string" && v.length === 36 && /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/iu.test(v);');
globalThis.refinementHttp=async(...args)=>{wire=args;return {refinement:response};};
const api=await import('data:text/javascript;base64,'+Buffer.from(ts.transpileModule(client,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText).toString('base64'));
assert(api.validatePrivateTeachingRefinement(review(),RID,REQUEST,SHEET));
for(const mutate of [v=>v.replica_id=ITEM,v=>v.request_id=ITEM,v=>v.sheet_id=ITEM,v=>v.sheet_hash='bad',v=>v.private_text_epoch='9223372036854775808',v=>v.field='identityWho',v=>v.value='x'.repeat(4001),v=>v.value='bad\ud800',v=>v.value='bad\u0000',v=>v.can_save=false]){const v=review();mutate(v);assert.throws(()=>api.validatePrivateTeachingRefinement(v,RID,REQUEST,SHEET));}check('actual public review validator refuses foreign scope, invalid bounds and authority shape');
for(const v of ['', ' ', 'x'.repeat(4001),'x\ud800','x\u0000'])assert.equal(api.validPrivateTeachingValue(v),false);assert.equal(api.validPrivateTeachingValue('x'.repeat(3998)+'🙂'),true);check('actual input validator preserves scalar pairs and enforces4000UTF16');
const basis=review();currentHash=NEW;epoch='2';value='relation -> diagram';response={...review(),saved:true};await api.savePrivateTeachingRefinement('synthetic-a',basis,{value},new AbortController().signal);assert.deepEqual(Object.keys(JSON.parse(wire[2].body)).sort(),['expected_private_text_epoch','expected_sheet_hash','field','op','replica_id','request_id','sheet_id','value']);
for(const mutate of [v=>delete v.saved,v=>v.private_text_epoch='3',v=>v.sheet_hash=H,v=>v.value='different',v=>v.can_save=true]){response={...review(),saved:true};mutate(response);await assert.rejects(api.savePrivateTeachingRefinement('synthetic-a',basis,{value},new AbortController().signal));}check('actual save transport validates commitment and receipt before success');
const resultClient=readFileSync(join(root,'src/studio/privateTextRehearsalApi.ts'),'utf8').replace('import { replicaRequest } from "./replicaApi";','const replicaRequest = () => {throw Error("source validator must not fetch");};');
const resultApi=await import('data:text/javascript;base64,'+Buffer.from(ts.transpileModule(resultClient,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext}}).outputText).toString('base64'));
resultMode='stale-complete';assert(resultApi.validatePrivateTextResult(result(),RID,REQUEST));
for(const mutate of [v=>v.can_review_teaching=false,v=>v.state='pending',v=>v.state='uncertain',v=>v.state='withdrawn',v=>v.failure_code='rehearsal_source_unavailable',v=>v.replica_id=ITEM,v=>v.request_id=ITEM,v=>{v.state='withdrawn';delete v.consent;delete v.source;delete v.failure_code;v.billing_state='unknown';}]){const v=result();mutate(v);assert.throws(()=>resultApi.validatePrivateTextResult(v,RID,REQUEST));}resultMode='complete';check('actual result validator permits marker only for exact scoped stale blocked response');
if(process.argv.includes('--source-only')){console.log(`${checks.length} source groups passed`);process.exit(0);}
const old=execFileSync('git',['show','43230e5e:src/studio/PrivateTextRehearsal.tsx'],{cwd:root,encoding:'utf8'});
// Execute the actual old complete-only conditional with all other current code.
const currentPanel=readFileSync(join(root,'src/studio/PrivateTextRehearsal.tsx'),'utf8');
const recoveryGuard='(result?.state === "complete" || result?.state === "blocked" && result.can_review_teaching === true)';
assert.equal(currentPanel.split(recoveryGuard).length-1,1);
const oldRecovery=currentPanel.replace(recoveryGuard,'result?.state === "complete"').replace('recoveryOnly={result.state !== "complete"} ','');
writeFileSync(join(art,'old-complete-only-result.tsx'),oldRecovery);
try{
 const built=await build({root,configFile:false,logLevel:'silent',build:{write:false,minify:true,rolldownOptions:{input:join(root,'evals/private-teaching-refinement/host.html')}},plugins:[{name:'old-missing-action',resolveId(id){if(id==='virtual:old-private-result')return '\0old-private-result.tsx';if(id==='virtual:old-recovery-private-result')return '\0old-recovery-private-result.tsx';},load(id){const source=id==='\0old-private-result.tsx'?old:id==='\0old-recovery-private-result.tsx'?oldRecovery:null;if(source!==null)return source.replace(/(from\s*|import\s*|import\()(["'])(\.\/?[^"']+)\2/g,(_,p,q,r)=>p+q+resolve(root,'src/studio',r).replaceAll('\\','/')+q);}}]});
 const assets=new Map(built.output.map(a=>['/'+a.fileName,a.type==='chunk'?a.code:a.source]));
 server=createServer(async(req,res)=>{const url=new URL(req.url,'http://fixture');const send=(code,data)=>{if(!res.destroyed){res.writeHead(code,{'content-type':'application/json','cache-control':'no-store'});res.end(JSON.stringify(data));}};
  if(url.pathname.startsWith('/api/')){
   let body='';for await(const chunk of req)body+=chunk;body=body?JSON.parse(body):{};const op=url.searchParams.get('op')||body.op,rid=url.searchParams.get('replica_id')||body.replica_id,request=url.searchParams.get('request_id')||body.request_id;
   requests.push({path:url.pathname,op,method:req.method,body,replica:rid,request,auth:req.headers.authorization});
   assert.ok(['Bearer synthetic-a','Bearer synthetic-b'].includes(req.headers.authorization));
   if(url.pathname==='/api/replica-text-rehearsal'){
    if(op==='readiness')return send(200,{readiness:readiness(rid)});
    if(op==='result')return send(200,{rehearsal:result(rid,request)});
    if(op==='ask'){assert.equal(body.expected_snapshot_hash,NEXT);assert.notEqual(body.request_id,REQUEST);assert.equal(new URL(req.headers.referer).searchParams.get('rehearsal_request'),body.request_id);assert.deepEqual(body.attestations,Object.fromEntries(statements.map(s=>[s.id,true])));return send(200,{rehearsal:result(rid,request)});}
    throw Error('unexpected rehearsal mutation '+op);
   }
   assert.equal(url.pathname,'/api/teacher-sheet');assert.equal(op,'private_refinement');
   if(req.method==='GET'){
    if(scenario==='read-error')return send(503,{error:'private_refinement_review_unavailable'});
    const snapshot=review(rid,request);if(scenario==='wrong-read')snapshot.request_id=ITEM;if(scenario==='invalid-recovery-editable'){snapshot.can_save=true;delete snapshot.blocker;}
    if(scenario==='late-read'){pending.push(()=>send(200,{refinement:snapshot}));return;}
    return send(200,{refinement:snapshot});
   }
   assert.equal(body.replica_id,RID);assert.equal(body.request_id,REQUEST);assert.equal(body.sheet_id,SHEET);assert.equal(body.expected_sheet_hash,H);assert.equal(body.expected_private_text_epoch,'1');assert.equal(body.field,'explanationOrder');
   assert.equal('draft' in body,false);assert.equal('attestations' in body,false);
   if(body.clear===true){assert.equal('value' in body,false);}else{assert.equal('clear' in body,false);assert.equal(typeof body.value,'string');}
   if(scenario==='uncertain-unchanged')return send(503,{error:'private_refinement_save_uncertain'});
   if(scenario==='conflict'){value='A newer explicit teaching order';currentHash=NEW;epoch='2';return send(409,{error:'private_refinement_conflict'});}
   value=body.clear===true?null:body.value;currentHash=NEW;epoch='2';const next={...review(rid,request),saved:true};
   if(scenario==='uncertain-applied')return send(503,{error:'private_refinement_save_uncertain'});
   if(scenario==='wrong-save')next.sheet_hash=H;
   if(scenario==='late-save'){pending.push(()=>send(200,{refinement:next}));return;}
   return send(200,{refinement:next});
  }
  const asset=assets.get(url.pathname);if(asset!==undefined){res.writeHead(200,{'content-type':extname(url.pathname)==='.html'?'text/html':extname(url.pathname)==='.css'?'text/css':'text/javascript'});return res.end(asset);}res.writeHead(404).end();
 });await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin=`http://127.0.0.1:${server.address().port}`;
 browser=await chromium.launch({headless:true});
 const until=async fn=>{const deadline=Date.now()+5000;while(!fn()){if(Date.now()>deadline)throw Error('bounded fixture barrier');await new Promise(r=>setTimeout(r,10));}};
 const count=op=>requests.filter(r=>r.op===op&&r.method==='POST').length;
 for(const width of [390,1440]){
  const ctx=await browser.newContext({viewport:{width,height:900},reducedMotion:'reduce'}),page=await ctx.newPage();page.on('pageerror',e=>errors.push(e.message));await page.route('**/*',r=>r.request().url().startsWith(origin)?r.continue():r.abort());
  const open=async(extra='')=>{scenario='normal';resultMode='complete';value=initial;currentHash=H;epoch='1';pending=[];requests=[];await page.goto(origin+'/evals/private-teaching-refinement/host.html?replica='+RID+'&rehearsal_request='+REQUEST+extra);await page.getByRole('heading',{name:'Your private text answer'}).waitFor();};
  const edit=async()=>{await page.getByRole('button',{name:'Adjust how I explain',exact:true}).click();await page.locator('#ptr-explanation-order').waitFor();};
  const fill=async()=>{await page.locator('#ptr-explanation-order').fill('known quantities -> diagram -> relation -> unit check');};
  const save=()=>page.getByRole('button',{name:'Save guidance',exact:true}).click();
  for(const clear of [false,true]){
   await open();await edit();if(!clear)await fill();scenario='uncertain-applied';
   if(clear)await page.getByRole('button',{name:'Remove saved guidance',exact:true}).click();else await save();
   await page.getByRole('alert').waitFor();const expected=value;assert.equal(count('private_refinement'),1);
   scenario='normal';resultMode='stale-complete';const boundary=requests.length;
   await page.reload();await page.getByRole('heading',{name:'Check your private request.'}).waitFor();
   assert.equal(requests.slice(boundary).some(r=>r.op==='private_refinement'),false);
   assert.equal(await page.getByRole('button',{name:'Adjust how I explain',exact:true}).count(),0);
   await page.getByRole('button',{name:'Check saved guidance',exact:true}).click();await page.getByText('Current explanation order',{exact:true}).click();
   assert.equal(await page.locator('.ptr-guidance-text').innerText(),expected||'No explanation order is saved.');
   assert.equal(await page.locator('#ptr-explanation-order').count(),0);assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).count(),0);assert.equal(await page.getByRole('button',{name:'Remove saved guidance',exact:true}).count(),0);
   assert.equal(await page.getByText('Saved to private draft.',{exact:true}).count(),0);assert.equal(count('private_refinement'),1);assert.equal(count('ask'),0);
  }check(width+': lost save and clear survive reload through explicit read-only GET without retry or receipt claim');
  const savedUrl=page.url();await page.goto(savedUrl+'&oldRecovery=1');await page.getByRole('heading',{name:'Check your private request.'}).waitFor();
  assert.equal(await page.getByRole('button',{name:'Check saved guidance',exact:true}).count(),0);await page.goto(savedUrl);await page.getByRole('button',{name:'Check saved guidance',exact:true}).waitFor();check(width+': executed old complete-only conditional loses recovery while current condition restores it');
  scenario='invalid-recovery-editable';await page.getByRole('button',{name:'Check saved guidance',exact:true}).click();await page.getByRole('alert').waitFor();assert.equal(await page.locator('#ptr-explanation-order').count(),0);assert.equal(count('private_refinement'),1);assert.equal(count('ask'),0);check(width+': malformed editable recovery GET cannot expose form or POST');
  for(const mode of ['unmarked-stale','source-blocked','pending','uncertain','withdrawn']){scenario='normal';resultMode=mode;await page.reload();await page.locator('.ptr-result h2').waitFor();assert.equal(await page.getByRole('button',{name:'Check saved guidance',exact:true}).count(),0);assert.equal(await page.getByRole('button',{name:'Adjust how I explain',exact:true}).count(),0);}check(width+': unmarked stale, source blocked, pending, uncertain and withdrawn expose no correction caller');
  for(const what of ['replica','token','request']){resultMode='stale-complete';scenario='late-read';await page.reload();await page.getByRole('button',{name:'Check saved guidance',exact:true}).click();await until(()=>pending.length>0);await page.evaluate(k=>window.refinementProbe.change(k),what);await page.getByRole('button',{name:'Check saved guidance',exact:true}).waitFor();pending.splice(0).forEach(f=>f());await page.waitForLoadState('networkidle');assert.equal(await page.locator('.ptr-guidance-text').count(),0);assert.equal(await page.locator('#ptr-explanation-order').count(),0);assert.equal(count('ask'),0);}check(width+': delayed recovery GET cannot install old guidance after owner, replica or request replacement');
  if(process.argv.includes('--recovery-only')){await ctx.close();continue;}
  for(const interaction of ['pointer','keyboard']){
   await open();await edit();await fill();scenario='late-save';await save();await until(()=>pending.length>0);
   if(interaction==='pointer')await page.locator('.ptr-heading h1').click();else await page.keyboard.press('Tab');
   const focusedBefore=await page.evaluate(()=>({tag:document.activeElement?.tagName,text:document.activeElement?.textContent?.slice(0,100)}));pending.splice(0).forEach(f=>f());await page.getByText('Saved to private draft.',{exact:true}).waitFor();
   assert.notEqual(await page.evaluate(()=>document.activeElement?.textContent),'Prepare another question');
   writeFileSync(join(art,width+'-'+interaction+'-focus-preserved.json'),JSON.stringify({focusedBefore,focusedAfter:await page.evaluate(()=>({tag:document.activeElement?.tagName,text:document.activeElement?.textContent?.slice(0,100)}))}));
  }check(width+': pending save cannot steal focus after pointer or keyboard interaction');
  if(process.argv.includes('--focus-only')){await ctx.close();continue;}
  await open('&old=1');assert.equal(await page.getByRole('button',{name:'Adjust how I explain'}).count(),0);assert.equal(count('private_refinement'),0);check(width+': exact old completed result has no refinement caller');
  await open();assert.equal(requests.some(r=>r.op==='private_refinement'),false);assert.equal(count('ask'),0);await edit();assert.equal(await page.locator('#ptr-explanation-order').inputValue(),initial);assert.equal(count('private_refinement'),0);assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).isDisabled(),true);assert.equal(await page.evaluate(()=>document.activeElement?.id),'ptr-refinement-title');writeFileSync(join(art,`${width}-focus.json`),JSON.stringify(await page.evaluate(()=>({tag:document.activeElement?.tagName,id:document.activeElement?.id,text:document.activeElement?.textContent?.slice(0,100)}))));await page.screenshot({path:join(art,`${width}-edit.png`)});check(width+': explicit action reads current result-bound field without mutation');
  await fill();await save();await page.getByText('Saved to private draft.',{exact:true}).waitFor();assert.equal(count('private_refinement'),1);assert.equal(count('ask'),0);assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).count(),0);assert.equal(await page.evaluate(()=>document.activeElement?.textContent),'Prepare another question');await page.screenshot({path:join(art,`${width}-saved.png`)});
  await page.getByRole('button',{name:'Prepare another question',exact:true}).click();await page.locator('#ptr-question').waitFor();assert.equal(await page.locator('.ptr-attestation input:checked').count(),0);assert.equal(await page.getByRole('button',{name:'Ask privately',exact:true}).isDisabled(),true);assert.equal(count('ask'),0);assert.equal(new URL(page.url()).searchParams.has('rehearsal_request'),false);await page.locator('#ptr-question').fill('Explain the period again using the saved order.');for(const box of await page.locator('.ptr-attestation input').all())await box.check();await page.getByRole('button',{name:'Ask privately',exact:true}).click();await page.getByRole('heading',{name:'Your private text answer'}).waitFor();assert.equal(count('ask'),1);assert.ok(requests.filter(r=>r.op==='readiness').length>=2);check(width+': explicit save -> refreshed readiness -> fresh attestations -> actual new question POST');
  await open();await edit();await page.locator('#ptr-explanation-order').fill('');assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).isDisabled(),true);await page.getByRole('button',{name:'Remove saved guidance',exact:true}).click();await page.getByText('Guidance removed from your private draft.',{exact:true}).waitFor();assert.equal(value,null);assert.deepEqual(requests.find(r=>r.op==='private_refinement'&&r.method==='POST').body.clear,true);check(width+': blank is not deletion; explicit removal sends clear without value');
  await open();await edit();await fill();scenario='uncertain-applied';await save();await page.getByRole('alert').waitFor();assert.equal(await page.getByText('Saved to private draft.',{exact:true}).count(),0);assert.equal(count('private_refinement'),1);assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).count(),0);scenario='normal';await page.getByRole('button',{name:'Check saved guidance'}).click();await page.getByText('Your private draft contains this guidance.',{exact:true}).waitFor();assert.equal(count('private_refinement'),1);assert.equal(count('ask'),0);check(width+': uncertain applied save uses explicit GET readback, never receipt claim or retry');
  await open();await edit();await fill();scenario='uncertain-unchanged';await save();await page.getByRole('alert').waitFor();scenario='normal';await page.getByRole('button',{name:'Check saved guidance'}).click();await page.getByText('The saved guidance is unchanged. Review your edit before saving again.').waitFor();assert.equal(await page.locator('#ptr-explanation-order').inputValue(),'known quantities -> diagram -> relation -> unit check');assert.equal(count('private_refinement'),1);await save();await page.getByText('Saved to private draft.',{exact:true}).waitFor();assert.equal(count('private_refinement'),2);check(width+': unchanged readback preserves typed edit and requires a separate explicit retry');
  await open();await edit();await fill();scenario='conflict';await save();await page.getByRole('alert').waitFor();scenario='normal';await page.getByRole('button',{name:'Check saved guidance'}).click();await page.getByText('This draft changed after the test. Start a new test before changing it again.').waitFor();await page.getByText('Current explanation order',{exact:true}).click();assert.equal(await page.locator('.ptr-guidance-text').innerText(),'A newer explicit teaching order');assert.equal(await page.getByRole('button',{name:'Save guidance',exact:true}).count(),0);assert.equal(count('private_refinement'),1);check(width+': stale draft conflict reads actual newer field and cannot overwrite it');
  for(const failure of ['read-error','wrong-read']){await open();scenario=failure;await page.getByRole('button',{name:'Adjust how I explain'}).click();await page.getByRole('alert').waitFor();assert.equal(await page.locator('#ptr-explanation-order').count(),0);assert.equal(count('private_refinement'),0);}check(width+': unavailable or foreign review never becomes an empty editable field');
  await open();await edit();await fill();scenario='wrong-save';await save();await page.getByRole('alert').waitFor();assert.equal(await page.getByText('Saved to private draft.',{exact:true}).count(),0);check(width+': malformed save commitment enters readback instead of success');
  for(const what of ['replica','token','request']){await open();scenario='late-read';await page.getByRole('button',{name:'Adjust how I explain'}).click();await until(()=>pending.length>0);await page.evaluate(k=>window.refinementProbe.change(k),what);await page.getByRole('button',{name:'Adjust how I explain'}).waitFor();pending.splice(0).forEach(f=>f());await page.waitForLoadState('networkidle');assert.equal(await page.locator('#ptr-explanation-order').count(),0);assert.equal(count('private_refinement'),0);}check(width+': delayed reads cannot reopen editor after replica, token or request replacement');
  for(const what of ['replica','token','request','hide']){await open();await edit();await fill();scenario='late-save';await save();await until(()=>pending.length>0);await page.evaluate(k=>k==='hide'?window.refinementProbe.hide():window.refinementProbe.change(k),what);if(what!=='hide')await page.getByRole('button',{name:'Adjust how I explain'}).waitFor();const boundary=requests.length;pending.splice(0).forEach(f=>f());await page.waitForLoadState('networkidle');assert.equal(await page.getByText('Saved to private draft.',{exact:true}).count(),0);assert.equal(count('ask'),0);assert.equal(requests.slice(boundary).some(r=>r.op==='readiness'&&r.replica===RID&&r.auth==='Bearer synthetic-a'),false);}check(width+': delayed save cannot update, refresh old scope or generate after replacement/unmount');
  await open();await edit();await fill();await page.locator('#ptr-explanation-order').focus();await page.keyboard.press('Tab');assert.equal(await page.evaluate(()=>document.activeElement?.textContent),'Save guidance');await page.keyboard.press('Enter');await page.getByText('Saved to private draft.',{exact:true}).waitFor();assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);assert.equal(await page.evaluate(()=>matchMedia('(prefers-reduced-motion: reduce)').matches),true);check(width+': keyboard save, responsive fit and reduced-motion environment');
  await ctx.close();
 }
 assert.deepEqual(errors,[]);writeFileSync(join(art,'result.json'),JSON.stringify({at:new Date().toISOString(),checks,errors,scope:'Actual mounted result/refinement/API/new-question callers with synthetic HTTP; no SQL, actual inference, full shell or quality acceptance.'},null,2));console.log(`${checks.length} groups passed; ${art}`);
}catch(e){writeFileSync(join(art,'failure.json'),JSON.stringify({checks,errors,error:String(e.stack||e)},null,2));throw e;}finally{await browser?.close();server?.closeAllConnections();if(server)await new Promise(r=>server.close(r));}
