// Bundle entry for the A4 forget battery — the REAL client-side half of a
// forget (scope resolution), from source, rebuilt on every run.
export { resolveForget, messagesAfterForget } from "../../src/engine/memory";
