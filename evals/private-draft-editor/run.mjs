// Actual mounted editors/API wrapper with synthetic localhost draft responses.
// No database, private inference, full Studio shell or publication acceptance.
import { launchSuiteBrowser } from "../rehearsal/browser.mjs";
import assert from 'node:assert/strict';
import {mkdirSync,writeFileSync,readFileSync} from 'node:fs';
import {join,resolve,extname} from 'node:path';
import {fileURLToPath} from 'node:url';
import {createServer} from 'node:http';
import {build} from 'vite';
import {chromium} from 'playwright';
import {checkDisclosures} from './disclosures.mjs';
import {INVALID_DRAFT_CASES} from '../private-draft-invalid-display.mjs';
const root=fileURLToPath(new URL('../../',import.meta.url)),art=join(root,'scratchpad/private-draft-editor',String(Date.now()));mkdirSync(art,{recursive:true});
const rid='10000000-0000-4000-8000-000000000001';
const minimal={name:'Anjali',identityWho:'Physics teacher',subjectDomain:'physics'};
let raw={...minimal},posts=[],gets=[],browser,server;const checks=[],errors=[],measurements=[];
// blobs from commits c56cadfe72a20ee02781485752d8d67fcfc6fb21 and
// 3db85f82f9491322a8db2a62556cf39be8234937, moved to committed fixtures
// (context/rejected.md#ci-shallow-checkout-starved-the-history-reading-suites).
const old=Object.fromEntries(['creatorStudio','studio'].map(l=>[l,readFileSync(join(root,`evals/private-draft-editor/fixtures/c56cadfe/src__${l}__TeacherSheetStudio.tsx`),'utf8')]));
const densityOld=Object.fromEntries(['creatorStudio','studio'].map(l=>[l,readFileSync(join(root,`evals/private-draft-editor/fixtures/3db85f82/src__${l}__TeacherSheetStudio.tsx`),'utf8')]));
const check=n=>{checks.push(n);console.log('PASS '+n);};
try{
 const built=await build({root,configFile:false,logLevel:'silent',build:{write:false,minify:true,rolldownOptions:{input:join(root,'evals/private-draft-editor/host.html')}},plugins:[{name:'exact-old-editor',resolveId(id){if(id==='virtual:density-old-creator')return '\0density-old-creator.tsx';if(id==='virtual:density-old-studio')return '\0density-old-studio.tsx';if(id==='virtual:old-creator')return '\0old-creator.tsx';if(id==='virtual:old-studio')return '\0old-studio.tsx';},load(id){if(id==='\0density-old-creator.tsx'||id==='\0density-old-studio.tsx'){const lane=id==='\0density-old-creator.tsx'?'creatorStudio':'studio';return densityOld[lane].replace(/(from\s*|import\s*|import\()(["'])(\.\.?\/[^"']+)\2/g,(_,p,q,r)=>p+q+resolve(root,'src',lane,r).replaceAll('\\','/')+q);}const lane=id==='\0old-creator.tsx'?'creatorStudio':id==='\0old-studio.tsx'?'studio':null;if(lane)return old[lane].replace(/(from\s*|import\s*|import\()(["'])(\.\.?\/[^"']+)\2/g,(_,p,q,r)=>p+q+resolve(root,'src',lane,r).replaceAll('\\','/')+q);}}]});
 const assets=new Map(built.output.map(x=>['/'+x.fileName,x.type==='chunk'?x.code:x.source]));
 server=createServer(async(req,res)=>{const url=new URL(req.url,'http://fixture');const send=(code,data)=>{res.writeHead(code,{'content-type':'application/json','cache-control':'no-store'});res.end(JSON.stringify(data));};
  if(url.pathname==='/api/teacher-sheet'){
   assert.equal(req.headers.authorization,'Bearer synthetic-owner');
   if(req.method==='GET'){assert.equal(url.searchParams.get('replica_id'),rid);gets.push(req.url);send(200,{sheet:{draft:raw,status:'draft',version:'',sheet_id:'20000000-0000-4000-8000-000000000001',updated_at:null}});return;}
   let body='';for await(const chunk of req)body+=chunk;const value=JSON.parse(body);assert.equal(value.op,'save_draft');assert.equal(value.replica_id,rid);posts.push(value);raw=value.draft;send(200,{ok:false,errors:[{field:'cloneDisclosureFact',code:'missing'}],sheet:{draft:raw,status:'draft',updated_at:null}});return;
  }
  const asset=assets.get(url.pathname);if(asset!==undefined){res.writeHead(200,{'content-type':extname(url.pathname)==='.html'?'text/html':extname(url.pathname)==='.css'?'text/css':'text/javascript'});res.end(asset);return;}
  res.writeHead(404).end();
 });await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin=`http://127.0.0.1:${server.address().port}`;
 browser=await launchSuiteBrowser("private-draft-editor");
 for(const width of [390,1440]){
  const context=await browser.newContext({viewport:{width,height:900},reducedMotion:'reduce'}),page=await context.newPage();page.on('pageerror',e=>errors.push(e.message));await page.route('**/*',r=>r.request().url().startsWith(origin)?r.continue():r.abort());
  const open=async(suffix,draft=minimal)=>{posts=[];gets=[];raw=structuredClone(draft);await page.goto(origin+'/evals/private-draft-editor/host.html'+suffix);await page.getByRole('button',{name:'Meet it',exact:true}).click();};
  if(!process.argv.includes('--invalid-only'))await checkDisclosures({page,width,open,posts:()=>posts,gets:()=>gets,art,check,measurements});
  if(process.argv.includes('--disclosures-only')){await context.close();continue;}
  for(const lane of ['creator','studio']){
   const suffix=lane==='studio'?'&studio=1':'';
   for(const [label,change]of INVALID_DRAFT_CASES){
    const invalid={...minimal,...change,unknownOwnerField:{keep:['exact',null,7]}};
    await open('?current=1'+suffix,invalid);await page.locator('#teacher-sheet-studio').waitFor();await page.locator('.draft-invalid-notice').waitFor();assert.equal(await page.getByRole('alert').count(),0);assert.equal(posts.length,0);
    await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts[0].draft,invalid,label+' untouched save');
    await page.locator('#syllabus-scope').fill('An unrelated explicit edit');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts.at(-1).draft,{...invalid,syllabusScope:'An unrelated explicit edit'});
   }check(`${width}/${lane}: five actual saveable malformed drafts render and preserve exact raw values on untouched and unrelated saves`);
   const malformedLists={...minimal,subjectStrands:[null,'mechanics'],doubtEscalationLadder:[{bad:true}],analogyBank:[null],unknownOwnerField:{keep:['exact']}};
   await open('?current=1'+suffix,malformedLists);await page.locator('.draft-invalid-notice').waitFor();assert.equal(await page.locator('.syllabus-chapters input:enabled').count(),0);assert.equal(await page.locator('.create-row input').isDisabled(),true);assert.equal(await page.locator('.create-row button').isDisabled(),true);
   await page.locator('#subject-domain').selectOption('chemistry');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts[0].draft,{...malformedLists,subjectDomain:'chemistry'});check(`${width}/${lane}: malformed list controls stay disabled and subject edits preserve their raw saved values`);
   await page.getByRole('button',{name:'Replace chapter list',exact:true}).focus();await page.keyboard.press('Enter');assert.equal(await page.evaluate(()=>document.activeElement===document.querySelector('.syllabus-chapters input')),true);assert.equal(posts.length,1);await page.locator('.syllabus-chapters input').first().check();
   await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);let changed=posts.at(-1).draft;assert.equal(changed.subjectStrands.length,1);assert.equal(typeof changed.subjectStrands[0],'string');assert.deepEqual(changed.doubtEscalationLadder,malformedLists.doubtEscalationLadder);assert.deepEqual(changed.analogyBank,[null]);assert.deepEqual(changed.unknownOwnerField,malformedLists.unknownOwnerField);
   await page.locator('.syllabus-chapters input').first().uncheck();await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts.at(-1).draft.subjectStrands,[]);check(`${width}/${lane}: explicit chapter replacement enables safe keyboard checkbox edits and changes only its field`);
   const writesBeforeReset=posts.length;await page.getByRole('button',{name:'Replace doubt steps',exact:true}).click();assert.equal(await page.evaluate(()=>document.activeElement===document.querySelector('.create-row input')),true);assert.equal(posts.length,writesBeforeReset);await page.locator('.create-row input').fill('Identify known values');await page.keyboard.press('Enter');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts.at(-1).draft.doubtEscalationLadder,['Identify known values']);assert.deepEqual(posts.at(-1).draft.analogyBank,[null]);await page.locator('.ladder-list button').click();await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts.at(-1).draft.doubtEscalationLadder,[]);assert.equal(await page.locator('.teacher-sheet-ingested-grid').getByRole('button').count(),0);assert(await page.locator('.teacher-sheet-ingested-grid').innerText().then(t=>t.includes('Saved value needs review.')));check(`${width}/${lane}: explicit doubt-list replacement supports add/remove without erasing invalid read-only or unknown fields`);
   const invalidScalars={...minimal,name:{keep:'name'},subjectDomain:'biology',strictness:'2',warmth:99,syllabusScope:{keep:true},identityLife:[null],boundaryParagraph:{keep:true},unknownOwnerField:{keep:['exact']}};
   await open('?current=1'+suffix,invalidScalars);await page.locator('.draft-invalid-notice').waitFor();for(const id of ['subject-domain','strictness','warmth','syllabus-scope','identity-life']){assert.equal(await page.locator('#'+id).inputValue(),'');assert.equal(await page.locator('#'+id).getAttribute('aria-invalid'),'true');}
   await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts[0].draft,invalidScalars);await page.locator('#strictness').selectOption('0');await page.locator('#identity-life').fill('An explicit teaching boundary');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts.at(-1).draft,{...invalidScalars,strictness:0,identityLife:'An explicit teaching boundary'});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.screenshot({path:join(art,`${width}-${lane}-invalid-values.png`)});check(`${width}/${lane}: malformed scalars have honest review states and only explicitly edited fields change`);
   if(process.argv.includes('--invalid-only'))continue;
   await open('?old=1'+suffix);await page.getByRole('alert').filter({hasText:'Render failed:'}).waitFor();const failure=await page.getByRole('alert').innerText();assert.match(failure,/map/);assert.equal(posts.length,0);writeFileSync(join(art,`${width}-${lane}-old.json`),JSON.stringify({failure,raw}));await page.screenshot({path:join(art,`${width}-${lane}-old.png`)});check(`${width}/${lane}: exact old editor fails on supported three-field draft`);
   await open('?current=1'+suffix);await page.locator('#teacher-sheet-studio').waitFor();assert.equal(await page.getByRole('alert').count(),0);assert.ok(gets.length>=1);assert.equal(posts.length,0);assert.equal(await page.locator('#strictness').inputValue(),'');assert.equal(await page.locator('#warmth').inputValue(),'');assert.equal(await page.locator('#syllabus-scope').inputValue(),'');assert.equal(await page.locator('.ladder-list li').count(),0);assert.equal(await page.locator('input[type="checkbox"]:checked').count(),0);check(`${width}/${lane}: actual GET -> Meet renders with no implicit values or saves`);
   if(lane==='creator')await page.screenshot({path:join(art,`${width}-usable.png`)});
   const save=page.locator('.person-model-action button');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),save.click()]);assert.deepEqual(posts[0].draft,minimal);assert.equal(await page.evaluate(d=>window.editorProbe.validate(d).ok,posts[0].draft),false);check(`${width}/${lane}: untouched explicit save preserves exact minimal body and fails actual sheet validation`);
   const extra={...minimal,customOwnerNote:{keep:['exact']},explanationOrder:'diagram -> relation -> check',version:'private-v1'};raw=extra;await Promise.all([page.waitForResponse(r=>r.request().method()==='GET'),page.locator('.section-heading button').click()]);await page.locator('#syllabus-scope').fill('Class 11 mechanics');await page.locator('#strictness').selectOption('2');await page.locator('.syllabus-chapters input').first().check();const rung=page.locator('.create-row input');await rung.fill('identify known quantities');await rung.press('Enter');await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),save.click()]);const saved=posts.at(-1).draft;assert.deepEqual(saved.customOwnerNote,extra.customOwnerNote);assert.equal(saved.explanationOrder,extra.explanationOrder);assert.equal(saved.version,'private-v1');assert.equal(saved.syllabusScope,'Class 11 mechanics');assert.equal(saved.strictness,2);assert.equal(saved.subjectStrands.length,1);assert.deepEqual(saved.doubtEscalationLadder,['identify known quantities']);assert.equal('warmth' in saved,false);assert.equal('cloneDisclosureFact' in saved,false);assert.equal('consentArtifactId' in saved,false);assert.equal('boardVerbalisms' in saved,false);check(`${width}/${lane}: reload and explicit edits retain unrelated fields without saving display defaults or authority`);
   raw={};await Promise.all([page.waitForResponse(r=>r.request().method()==='GET'),page.locator('.section-heading button').click()]);await page.waitForFunction(()=>document.querySelector('#subject-domain')?.value==='');assert.equal(await page.locator('#strictness').inputValue(),'');assert.equal(await page.locator('#warmth').inputValue(),'');await save.focus();await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.keyboard.press('Enter')]);assert.deepEqual(posts.at(-1).draft,{});check(width+'/'+lane+': entirely empty draft has no selected subject or fabricated required values; native keyboard save remains empty');
   await open('?full=1'+suffix);await page.locator('#teacher-sheet-studio').waitFor();await Promise.all([page.waitForResponse(r=>r.request().method()==='POST'),page.locator('.person-model-action button').click()]);assert.deepEqual(posts[0].draft,await page.evaluate(()=>window.editorProbe.full));check(`${width}/${lane}: complete incumbent sheet round-trips unchanged`);
  }
  await open('?hi=1',{...minimal,subjectStrands:'bad',doubtEscalationLadder:[null],warmth:'4'});await page.locator('.draft-invalid-notice').waitFor();await page.getByRole('button',{name:'अध्यायों की सूची बदलें',exact:true}).waitFor();await page.getByRole('button',{name:'शंका के चरण बदलें',exact:true}).waitFor();assert.equal(await page.locator('#warmth option:checked').innerText(),'सेव की गई जानकारी जाँचें।');assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);check(`${width}: malformed field notices and explicit replacements are localized in Hindi`);
  if(!process.argv.includes('--invalid-only')){await open('?hi=1');await page.locator('#teacher-sheet-studio').waitFor();assert.equal(await page.locator('#strictness option:checked').innerText(),'अभी तय नहीं');assert.equal(await page.locator('#warmth option:checked').innerText(),'अभी तय नहीं');assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.screenshot({path:join(art,`${width}-hindi.png`)});check(`${width}: Hindi unset controls and responsive fit`);}
  await context.close();
 }
 assert.deepEqual(errors,[]);writeFileSync(join(art,'result.json'),JSON.stringify({at:new Date().toISOString(),checks,errors,measurements,scope:'Actual mounted editor/API wrappers, exact old source negative, synthetic HTTP. No full-shell, SQL or model acceptance.'},null,2));console.log(`${checks.length} groups passed; ${art}`);
}catch(e){writeFileSync(join(art,'failure.json'),JSON.stringify({checks,errors,error:String(e.stack||e)},null,2));throw e;}finally{await browser?.close();server?.closeAllConnections();if(server)await new Promise(r=>server.close(r));}
